# ============================================================
# 艾尔希亚跑商 · Git 忽略规则（多人协作）
# ============================================================

# ---- 运行时数据（服务器自动生成/更新，不入库）----
world.json                # 运行中的世界状态（时间/价格/广播，持续变化）
players/                  # 玩家存档目录（服务器运行时创建）
chat.json                 # 弹幕聊天记录

# ---- 日志 / 临时文件 ----
*.log
logs/
*.tmp
*.bak

# ---- 操作系统 / 资源管理器 ----
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# ---- IDE / 编辑器（按团队习惯取舍）----
.vscode/
.idea/
*.swp
*.swo
.project
.settings/

# ============================================================
# 说明：
# - default-world.json 是"初始世界模板"（服务器从它生成 world.json），
#   建议入库，保证所有协作者初始世界配置一致。
# - index.html（在线版）与 单机版/index.html 逻辑需同步维护，
#   修改任一份时请同步另一份（两版共用同一套核心逻辑）。
# ============================================================
