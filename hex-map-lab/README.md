# hex-map-lab —— 六边形 2.5D 跑商地图实验页

这是《地图系统重构方案》的**阶段 0+1 垂直切片**，一个独立的纯网页实验：用 Three.js 渲染手绘蜡笔沙盘风的六边形 2.5D 地图，复用原项目已有的 13 座城市与 21 条道路拓扑。

当前实验版的地表高度分**两层**（v2.8 阶段二）：逻辑层是**离散档位**（地块类型即高程 —— 水 / 平原 / 城市 = 基准面，丘陵 = 0.10 格 ≈ 2.2 单位），渲染层 = 档位 + 一层**圆润连绵的丘陵波**（世界坐标上的低频场，不是「一格一个包」）+ 河带走廊 + 泉湖碗。所以平原仍然是完全平的，丘陵只是「一点点连绵的波」（整体观感依旧平，只有高度感）；山体仍由簇级连续高度场生成并作为独立几何叠加在地表上，山格**不抬地表基座**。连续山格可以形成多主峰、鞍部、冲沟、岩台、山谷与不对称孤峰；相邻山格以共享的世界坐标高度场衔接，非山格、水体与河流不会被自动填平。

河流在共享角点构成的顶点图上沿格边寻路，并以同一条权威中心线同时驱动河面、地表河槽与山体切口。策划可用稀疏覆写指定 `mountainGorge`、`mountainPass`、`waterfall`、`blocked` 或 `dryValley` 等水路语义；峡谷与隘口已有真实侵蚀，且**山体网格本身会消费这个切口**（否则山壳会把整条穿山河重新盖住 —— 历史实测 171 个河道横断面里 169 个被岩石压住，见下文与方案 §15.21）；瀑布、干谷和阻断的专属视觉仍在后续范围。道路过水与跨河是桥、穿山是隧道；另有蜡笔连续描边、**贴图级混合的统一地表（草地 / 森林 / 农田 / 花田 / 岩壁按逐顶点权重混贴图与底色，交界处不再是一格一色，v2.9）**、五档道路（泥路 → 石子路 → 夯土车辙 → 石板 → 铁轨）、生态植被、村落房屋、贴地云影与飞鸟、**日夜 / 季节 / 天气**三轴环境系统，以及两名玩家「服务器权威状态 + 客户端插值」的移动链路。

## 独立性声明

本目录是独立产物，**不读写、不修改原项目的任何文件**。与项目的关系只有一处：`src/data/world-snapshot.js` 是 `map/world-map.json` 的一份只读快照（13 城 / 21 路 / viewBox 850×520 / worldSchema 972 / terrainSeed 20260812）。之所以内联成快照而不是运行时读取，是因为本页要支持双击 `file://` 打开，而浏览器禁止跨目录 fetch。

方案文档见 [地图系统重构方案（实验版）.md](./地图系统重构方案（实验版）.md)。

## 运行

双击 `index.html` 即可，无需构建、无需联网（Three.js 已放在 `vendor/`）。

如果浏览器对本地脚本有限制，也可以起一个静态服务：

```powershell
python -m http.server 8099
# 然后访问 http://127.0.0.1:8099/
```

操作方式：左键拖拽旋转视角，右键 / 中键 / Shift+左键平移，滚轮缩放，点击地块或城市查看详情并高亮。右侧面板可以切换正射 / 低透视相机，以及描边、道路、里程标签、植被、村落、城市、玩家、**山体**、**水面（海 / 河 / 湖）**、**河口三角洲**、**云影（贴地）**、**云雾（高空）**、飞鸟、网格线共十四个图层的显隐（v2.8 起海 / 河 / 泉已合并成同一份几何，「水面」只能整片开关；原「河流」那一格改成「河口三角洲」—— 同一个 `uDeltaOn` uniform 开关）；另有「山脉重掷」卡片（`重掷山脉` / `还原默认`，只换山脉分布，供策划对比不同效果，面板上会显示当前山脉种子号）、环境卡片（时段 / 季节 / 天气 / 自动轮播），「道路一览」卡片把五档道路各画一张顶视预览用于对照等级差异。**FPS 与网格统计的悬浮条固定在左上角**。

## 改参数就能改世界

世界的生成与外观集中在唯一的参数文件 `src/config/world-config.js`，策划改这一个文件即可，不需要动逻辑与渲染代码：

| 段 | 作用 |
| --- | --- |
| `terrain.landform*` | 地貌份额（水 / 低地 / 丘陵）、岛屿衰减、噪声采样尺度、类别平滑遍数 |
| `terrain.landuse*` | 用途份额（草地 / 农田 / 森林 / 花田）与其噪声通道 |
| `terrain.relief` | **山脉通道**：山脉份额 `ridgeShare`、噪声尺度（11 格）、城市距离衰减（`cityFadeStart/End`，2 → 7 格）、孤立山点软化遍数 |
| `terrain.relief.mountains` | **山体形状**：峰高包络 `peakHeight` / `ampScale`、孤峰比例 `loneScale`、簇级峰高增长 `peakHeightGrow`、脊带半宽与蜿蜒 `beltHalfWidth` / `beltWander` / `beltWidthJitter`、峰谷调制 `summitScale` / `summitFloor`、脊网 `crestStretch` / `crestSharp`、放射脊 `spurAmp`、岩台 `baseLevel`、轮廓收脚 `taperInner` / `taperOuter` / `outlineWobble`、**可见厚度下限 `footMin`**（v2.6，见「山体」一节）、山谷 `valley`、演示块 `demoMassif` |
| `terrain.relief.demoMassif` | 演示用的 4×5 手工山块（默认贴地图左下角，`col / row / w / h`）；优先级高于比例生成，但不计入目标份额 |
| `height` | **世界高度（v2.8 阶段二）**：档位表 `tiers`（水 / 平原 / 城市 = 基准面，丘陵 `0.10` 格）、山格的水文档位 `ridgeHydroTier`（引用 `tiers` 键名，不另写数值）、丘陵连绵波 `hillWave`（`scale` 波长 5 格 / `band` 过渡带 0.09 —— **不写幅度**，幅度由 `tiers.hill` 派生：均值 = 丘陵档、峰 = 2×）、岛屿衰减 `islandFalloff`、旧 `visualPeak`（保留兼容，已不参与地表高度） |
| `river` | **河道**：统一半宽 `width`、岸色带倍数 `wetScale / bankScale / floodScale`、避让倍数 `propsClearanceScale`、浅切槽 `channel.depth / widen`、**沿程阶梯下降 `stepSlope`（档间斜坡的沿河长度，格）与河带走廊 `corridor.band`（槽宽之外的平坦带，把河岸钉在局部河面上）**、支流参数、**穿山侵蚀 `gorge.depth/widen` 与 `pass.depth/widen`（`depth` = 核心开通度：1 表示整条水带完全开通）、覆写模式的路径偏好 `routeBias`**、**走不到水时的策略 `onDeadEnd`（`'drop'` 丢弃 / `'join'` 并入）**、**河口分流与冲积 `delta`（`branches / forkT / length / spread / widthScale / bend / alluvialRadius / alluvialStrength`）** |
| `water` | **水体表现（v2.7 重写）**：水位 `level`、水下地表 `depthShallow / depthDeep / depthRamp / shoreRamp`、画面深度过渡 `depthFade / alphaMin / depthResolution / depthFilter`、**`seaMinBodyTiles`（面积 ≥ 它才算「大片连续纯水」，是入海口的唯一判据）**、**共享表现段 `shared`**（波浪 `waveAmplitude / waveLength / waveSpeed / waveCross / waveDetail`、卡通光照 `bandSteps / bandSoftness / specular / highlightSteps`、吸收 `absorption / aoStrength / edgeDarken`、泡沫 `foamColor / foamNoise / shorelineMotion` —— **全部以 × hexSize 计**）、海 / 河 / 泉三个 profile（`depthFade / alphaMin / waveSpeedMul / absorptionMul / foamMul / foamWidth / flowStrength / renderBias`；河流另有 `mouthBlend`）、可选波浪高度图 `heightmap` |
| `environment` / `weather` / `season` / `dayNight` | 环境系统：默认时段 / 季节 / 天气、天气预设（晴 / 多云 / 雨 / 雾）、季节预设、日夜轮转周期 `dayNight.cycleSeconds` |
| `palette` | 全部配色：地形、水面、树种、花、作物、岩石、房屋、城市、五档道路、**蜡笔描边参数**、**顶点色交融权重**、天空、底座 |
| `ecology` | 每种地形搭配的物种、密度、每格株数、成簇半径、道路避让距离、生长度影响 |
| `village` | 每城房屋数量、分布半径、尺度、成簇概率 |
| `road` | 五档道路的里数阈值、宽度、**路面材质与构件**（`surface` / `rail` / `curb` / `ruts` / `scatter` / `irregularEdge`）、桥与栈桥余量、**隧道洞口尺寸**、**磨损速率（默认 0＝不演示）** |
| `props` | 道具状况衰减速率、修缮增幅、破败色（默认 0＝不演示） |
| `ambience` | 云雾数量/高度/速度/透明度、**贴地云影**（层数/透明度/漂移速度/纹理重复/覆盖范围/网格步长/抬升高度/贴图边长）、飞鸟数量/半径/速度 |
| `lighting` | 半球光、环境光、方向光、补光、阴影、雾 |
| `resources` | 各地形的资源产出（药草 / 谷物 / 橡木 / 染料 / 石材 / 海鱼） |

地形份额是按**排名分位**切分的：先把所有格子的噪声值排序，再按目标比例分配类型。这样做的好处是实际占比与配置几乎一致，不会因为噪声分布不均匀而跑偏。HUD 的「地图状态与生成比例」面板会同时显示目标比例与实际格数，方便校对。

山脉走的是**另一条通道**：带符号的大尺度噪声（`terrain.relief.scale = 11` 格）× 城市距离权重。挑选范围是「非水 **且** 离城 ≥ `cityFadeStart`（2 格）」的格子，按噪声值排序后取最高的 `ridgeShare`（0.42）那一批。噪声尺度放大到 11 格之后波峰拉成长脊，山脉因此连成脉而不是一堆孤立小山包。单格山**不被清理**：它是合法的孤峰，由 `loneScale` 控制峰高；旧版的「孤立山点软化」会把单格山抹掉，已连同参数一起删除。

山格之间的连接是**六邻接**（仅角接触不算同一山系），山簇 ID 由 seed 与排序后的 tile key 集合哈希得到，因此在策划改动其中一格之后，其余格仍属于同一山系。山体的实际形状由簇级高度场统一给出：脊带、主峰与次峰、鞍部、冲沟、岩台、山谷、孤峰对不对称性都来自这一处，渲染层只做采样与上色。

这条通道的种子是可换的（`world.reliefSeed`，见下文「山脉重掷」）：换了它，山格分布与山体形态一起变，而地貌 / 用途两条通道不动。⚠ v2.8 阶段二起这条通道**只有一个消费者**（山格排名）：丘陵起伏已改由 `HeightField.hillWave` 从**地貌噪声**通道算出，旧版「同一个通道值同时喂山格与丘陵 dome」的双消费者结构连同 `relVal` 一起删除。

**高度分两层（v2.8 阶段二）**：**逻辑基座**（离散）＝ 地块类型即高程（`height.tiers`：平原 / 城市 = 0、丘陵 = 0.10 格；**山格基座恒为 0**，不抬）；**渲染地表**（连续）＝ 基座之上叠一层**圆润连绵的波**（`height.hillWave`：世界坐标上的低频场，均值 = 丘陵档、峰值 = 2 × 丘陵档），再叠加河道走廊与泉湖碗。整套档位与波形只有一处实现（`HeightField`），地表查询只有一个入口 `world.heightAt(x, z)` —— 河流的浅切槽、丘陵波、泉湖碗都叠在里面（见下文「河流」）。

## 编辑器覆写与重建

世界生成、策划覆写与运行时状态彼此分离。覆写以稳定地块键组成稀疏数据，保存后以相同种子、配置与覆写重建会得到相同结果。当前支持的结构如下：

```js
{
  tiles: {
    "q,r": {
      terrain: "ridge",
      mountain: {
        style: "twinPeak",
        heightScale: 1.25,
        landmark: false,
        valley: "auto"
      },
      waterway: {
        mode: "mountainPass"
      }
    }
  }
}
```

`src/world/world-rebuild.js` 是纯逻辑事务：它依次构建世界、稳定山簇 ID、山体场、河流、道路、动态地块状态与派生规则，不触碰 DOM、Three.js、相机或 HUD。实验页启动和后续覆写都应走同一入口，避免两条构建链逐渐分叉。

页面加载后可从编辑器或调试环境调用：

```js
window.__hexLab.rebuild({ terrainOverrides: nextOverrides });
```

若要让 3D 引擎渲染**另一份地图**（不是内置快照），先替换数据源再重建 —— 这是工具接入的唯一注入缝，无需改动任何渲染 / 逻辑模块：

```js
HL.Data.useMap(mapFromWorldMapJson);   // cities / roads / viewBox / terrain.seed / worldSchema
window.__hexLab.rebuild({ terrainOverrides: nextOverrides });
HL.Data.useMap(null);                  // 还原为内置快照
```

除显式地块键，覆写还支持**按地形批量命中的规则**（`match` 支持 `terrain` / `landform`）：

```js
{
  rules: [
    { match: { terrain: 'ridge' }, set: { waterway: { mode: 'mountainGorge' } } }
  ]
}
```

规则存在的理由是：山格是**生成结果**，「把所有山格设成峡谷」如果只能逐格列举 key，就必须「先生成一遍拿到山格、再回头覆写」跑两遍构建。优先级是**规则 < 显式 key**，即规则是默认值、显式覆写是最终值；规则也会计入覆写 revision，否则改规则不会让山体场与河网的缓存失效。

默认地图上有一条演示规则 `river.demoWaterway`（`{ enabled: true, mode: 'mountainPass', target: 'ridge' }`），它走的就是上面这条管道，用来直接看到「河从山口穿过」；`enabled: false` 即回到完全自然的河网。

## 山脉重掷（策划对比用）

山脉通道的种子独立于世界种子：`world.reliefSeed` 默认等于 `seed + terrain.relief.seedOffset`，传别的值就只换山脉。之所以能把种子拆开，是因为那条脊状噪声通道现在**只有一个消费者**（山格排名 `ridgeVal`，见于 `hex-world.js` 的注释）：丘陵起伏在 v2.8 阶段二已改为**地形自己的连续场**（`height.tiers` + `HeightField.hillWave`，读地貌噪声通道），所以重掷山脉**不会顺带改掉丘陵起伏**（旧版两者共用这条通道，还有一份默认种子的 `relVal` 专供丘陵 dome，随 dome 一起删掉了）。

页面上是右侧的「山脉重掷」卡片（`重掷山脉` / `还原默认`），面板显示当前种子号、山格数、山簇数，日志里也会写一条。种子来自**确定性序列**（计数器 × 大质数），不使用 `Math.random()` —— 看到喜欢的分布时把种子号记下来即可复现。

```js
window.__hexLab.rerollMountains();      // 下一颗山脉种子
window.__hexLab.rerollMountains(12345); // 指定种子（复现某一次结果）
window.__hexLab.resetMountains();       // 回到默认分布
```

重掷会走一次完整的 `app.rebuild()`：山格分布与山体形态改变，依赖山格的河流与道路随之重算，而水 / 草 / 田 / 林 / 花的占比与逐格分布保持不变（实测非山格的 `terrain / landform / 档位` 零漂移，409 格全部一致）。山格总数只会在演示块（`relief.demoMassif`，20 格）的范围内浮动：排名部分的格数是固定的，手工演示块是「谁还不是山就改成山」，它与排名结果的重叠格数会随种子变化（实测 81 → 78 格）。

应用级重建会释放旧图层的 GPU 资源，替换 terrain、mountains、rivers、springs 等表现层，并重建深度预通道、山体 LOD、Picker 与旅行模拟。它会保留图层显隐、环境状态、相机控制器和仍有效的选中地块；旅行中的玩家路径目前不会迁移，正式运行时需要在道路覆写后补充安全迁移规则。若后续编辑器允许替换 seed、地图尺寸或完整快照，也应在这个接口扩展参数与相机范围迁移。

## 代码结构

按功能分层，每层一个目录，依赖方向单向向下。

```
index.html              入口：DOM 骨架 + 按依赖顺序加载模块
styles.css              页面与面板样式
vendor/three.min.js     Three.js r147（UMD，经典脚本）
src/config/
  world-config.js         唯一参数源（上表各段）
src/core/               基础能力
  event-bus.js            发布订阅总线（解耦各层）
  rng.js                  确定性随机与噪声（禁用 Math.random）
  hex.js                  轴向坐标 (q,r) 数学 + 角点 / 格边工具（共享角点、共享格边中点 edgeMid、方向向量；河线、山体基环与脊顶端点共用）
  proximity.js            最近采样点查询（3×3 网格哈希；路与河共用一份实现）
src/data/
  world-snapshot.js       项目地图快照（只读）
src/world/              纯逻辑，不依赖 DOM
  height-field.js         **高度场的唯一实现**（v2.8 阶段二）：档位表（`tiers` / `tierOf` / `baseTier`）、地貌噪声与丘陵阈值、丘陵掩码与**连绵波**（`hillWave`）。hex-world / river-builder / spring-layer / mountain-field / terrain-layer 五处都从这里取，不得自行推算
  hex-world.js            按比例生成地形与山脉通道、**逐地块档位 + 丘陵连绵波 + 河道走廊 + 泉湖碗**、离岸距离、**水下地表（敞水深度场 × 岸坡因子）**；heightAt 是唯一高度入口
  terrain-overrides.js    稀疏策划覆写与「按地形批量命中」规则：规范化、合并优先级（规则 < 显式 key）与稳定 revision
  terrain-rules.js        地貌语义规则与编辑器联动建议，供渲染层与生态查询
  mountain-cluster.js     山簇分析：仅以六邻接连接，按 seed 与排序 tile key 派生稳定山系 ID
  mountain-system.js      山体风格、峰群和水路模式的计划层，不直接生成几何
  mountain-field.js       山体高度场（**唯一真源**）：簇级窄脊带、低岩台、放射脊、山谷与河流切口；缓存依赖河流 revision
  river-builder.js        河流：顶点图上的严格下降（沿格边）、汇流、中心线索引、河槽和峡谷/隘口侵蚀
  world-rebuild.js        纯逻辑重建事务：统一装配 world、山簇、山体场、河流、道路、状态与规则
  road-builder.js         道路样条、五档分级、地块标记、桥（含跨河）/栈桥/隧道区段、避让索引
  city-graph.js           城市图与 Dijkstra（实验内部唯一实现）
  tile-state.js           动态状态层：生态基底、道路状况、道具状况、序列化
src/render/             表现层，只读世界模型与状态
  textures.js             程序化纹理（地表/蜡笔笔触/岩壁/**湿沙岸线**/五种路面/云影）与带轮廓的植被贴图
  ribbon.js               贴地扁带工具（路面 / 车辙 / 路缘 / 钢轨 / 桥面 / 洞内路面 / 河面共用；支持 ground + maxDrop 横向贴合地形）
  road-style.js           五档道路的视觉配方（路面材质表、构件清单、顶视预览）
  scene.js                渲染器 / 双相机 / 光照 / 天空渐变
  environment-state.js    环境状态源：日夜 / 季节 / 天气 / 自动轮播（唯一状态，HUD 与各层共用）
  environment-palette.js  环境色解析：基础色板 × 当前环境 → 各层的最终配色
  ground-material.js      **统一地表材质（v2.9）**：`MeshStandardMaterial` + `onBeforeCompile` 的**最小注入**（替换 `<map_fragment>` 一处），按逐顶点权重混合五张灰度贴图并乘逐槽位底色。留在标准材质上是为了**免费继承**雾 / 阴影贴图 / 光照 / tone mapping（换自写 `ShaderMaterial` 就得逐个手工接回去）
  terrain-layer.js        地表（**一份网格 + 一份材质**，顶点高度读 heightAt；逐槽位 splat 属性 `aSplatA` / `aSplatB`、「明暗比值」顶点色与共享顶点法线）、**水下地表** + 沙盘底座；并对外提供几何累加缓冲（`createBuf` / `appendGroupGeometry` / `bufToGeometry`），是「统一水面」拼几何的底子；槽位顺序与底色来源的唯一出处是 `SURFACE_SLOTS`
  mountain-layer.js       山体层：共享三角格网格器（相邻格复用同一批格点）+ 按位置焊接 + 表面/落地墙分工；按 `lod.details` 为每个山簇建**多级网格**，另供占位查询
  mountain-lod.js         山体 LOD 控制器：按相机与视口的**屏幕像素密度**选级 + 滞回 + 相机静止短路。不碰几何、不碰场
  water-surface.js        **统一水面**（v2.8）：海（按 `water.wave.subdiv` 加密）+ 河（含河口分流）+ 泉 / 湖圆盘写进**同一份几何**，配**唯一一份**水面材质。海面是唯一平面（恒为海面水位）；河 / 湖各取**自己所经地块的档位高度**（v2.8 阶段二：沿程阶梯下降，见下文）
  river-layer.js          河面几何追加器：圆滑化（Chaikin）、**末端收尖**（消「方头」）、逐采样点写河口因子 `aMouth`，把河带与分流带追加进统一水面的缓冲
  spring-layer.js         河源水体（泉眼 / 小湖）的圆盘几何追加器：极坐标圆盘 + 岸线扰动 + 涟漪 UV，同样追加进统一水面的缓冲
  water-depth.js          深度预通道（只画地表 + 山体）与**共享深度 uniform / GLSL** 的提供者：不再拥有颜色，也不注入任何材质
  water-material.js       **手绘卡通水体材质**：海 / 河 / 泉共用的**一份**自写 ShaderMaterial（片元波浪 + 顶点位移 + cel 色带 + 按真实水深的泡沫 + 河口混合 / 羽化 + 逐顶点水型档案）
  ink-layer.js            蜡笔描边（沿边连续笔触：共享站点、逐点抖动宽度与浓淡）、反转壳描边工具（**泡沫不再在这里**）
  grid-layer.js           六边形网格线（调试，默认关闭）
  road-layer.js           五档路面与构件（枕木/钢轨、路缘石、车辙、散石）、蜡笔边线、桥与栈桥与隧道洞口
  props-layer.js          多物种植被与岩石、边界过渡装饰簇、格内次生特征（水洼等）
  village-layer.js        村落房屋（反转壳描边 + 状况配色）
  city-layer.js           城市地标（实例化的基座/塔身/屋顶）
  player-layer.js         玩家棋子，只消费姿态数据
  ambience-layer.js       贴地云影（贴合地形的网格 + 滚动纹理 offset）与半透明云雾、飞鸟
src/interaction/        交互层，只发意图
  camera-control.js       轨道相机（旋转 / 平移 / 缩放，带阻尼）
  picker.js               射线拾取（地块与城市，区分点击与拖拽）
src/simulation/
  travel-sim.js           旅行模拟，扮演「服务器权威」那一侧
src/app/
  hud.js                  HTML 叠加面板，只通过事件总线发意图
  world-view.js           **3D 表现层装配事务**（建层 / 重建 / 释放 / 渲染一帧）——实验页与工具共用
  main.js                 装配入口与主循环（玩家、旅行模拟、HUD、环境控件）
```

命名约定：每个模块是一个 IIFE，只向全局挂一个 `HexLab` 命名空间，不在全局散落变量，也不使用 ES Module。这与原项目「经典脚本 + 顶层共享」的组织方式一致，好处是无需构建、`file://` 可直接运行。

> **被工具复用（只读）**：`tools/map-editor/` 的「地形模式」以相对路径引用本目录的 `src/**` 与 `vendor/three.min.js`，并通过 `HL.Data.useMap(map)` 把项目正式地图注入 3D 引擎，从而得到所见即所得的地形预览。本目录**不反向依赖** `tools/`，仍然是可独立双击运行的实验页。

## 视觉风格

对齐手绘蜡笔插画风（参考《筑梦颂》与手绘平涂地图）：

- **逐地块档位 + 丘陵连绵波（v2.8 阶段二）**：地表高度分两层。**逻辑层**是离散档位 —— 地块类型即高程（水 / 平原 / 城市 = 基准面，丘陵 = 0.10 格 ≈ 2.2 单位），写在 `tile.surfaceY` 上（旧版的 `cornerY` / `hillAmp` / `microNoise` 等字段已删）。**渲染层**在档位之上再叠两样东西：① 丘陵的**连绵波**（`HeightField.hillWave`，世界坐标上连续的低频场，均值 = 丘陵档、峰 = 2×），② 河带走廊与泉湖碗（钉在局部河面 / 档位上）。丘陵因此**不是**「一格一个圆包」—— 旧版逐格 dome + 格边归零的做法已删；现在整体观感仍然是平的，只有一点点圆润连绵的高度感。平原格心恒为 0（完全平，实测非平格心 0 个）；山脉**不再抬升地表**，仍是叠加在地表之上的独立模型。实测丘陵 81 格、格心均值 **2.189 ≈ 丘陵档 2.2**、峰 3.69 单位；旧参数 `height.visualPeak`（1.9 × 格半径 ≈ 41.8）已不参与地表高度，仅作兼容字段保留。地表高度只有一个查询入口 `world.heightAt(x, z)`，河流浅切槽与河岸走廊也叠加在里面。
- **真正的无缝**：同一个物理顶点在相邻地块里是**不同的顶点副本**，若颜色与法线各算一份，平地就会沿六边形边界露出色阶与明暗缝。因此顶点色与法线都按「世界位置 + 该处地块集合」统一计算，共享顶点上的颜色/法线数值完全一致（实测最大差 0）。这条是「不要分明的六边形」最重要的实现细节。
- **不同地形互相交融**：角点顶点的颜色取「压在该角上的 3 格」等权平均，于是色带在地块交界处整圈过渡；地块中心再按 `palette.blendWeights.center` 朝 6 邻平均色渗透，过渡因此从「只有一圈角点渐变」变成「格心就开始互相渗透」。注意**角点必须等权**：给自身更大权重、或把外圈并进来，三格就会各算一份，实测色差立刻回到 0.07~0.12，六边形色阶重现。
- **贴图级地形混合（v2.9）**：地表从「按地形分组的五张网格」改成**一份网格 + 一份材质**（`render/ground-material.js`）—— 草地 / 森林 / 农田 / 花田 / 岩壁各一张程序化灰度贴图，按**逐顶点权重**混合后乘**逐槽位底色**，于是地形交界是**贴图级**渐变，而不是两种纯色硬碰。底色取**配置的地形调色板**（与顶点色那份「明暗比值」的分子同源 ⇒ 纯色地块与改造前逐位一致，splat 只负责交界），⚠ **不吃环境色板**：`env.terrain.*` 是「向季节 tint 混色」后的值，而夏季 tint 是 `0xffffff`，等于把草地朝白混 28%，整图会被冲淡。岸线另有一条 0.9 格宽的**湿沙混合带**（由 `world.waterDistance` 的连续距离场驱动）。两条踩过的坑写在方案 §15.28：底色**不能**等到 `onBeforeCompile` 里现造（`applyEnvironment()` 在首次渲染**之前**就执行 ⇒ 注入落空 ⇒ **整图陆地全白**），色号**必须线性化**（明暗比值是线性空间的量，不线性化会过曝到 255、同样读成「发白」）。
- **水陆基色族隔离（v2.5）**：上面那条「等权平均」对**水 / 陆**必须例外。压在同角上的 3 格里只要有 1 格是水，陆地那一份就被掺进 1/3 的水色（`palette.terrain.water = 0x8cbac8`，偏青蓝），格心还会再按 `cb/6` 掺一点 —— 肉眼就是「**贴着水的陆地基本色变成了蓝的**」。而「一片纯水该是什么颜色」本该由**画面深度**单独决定，不该反过来去染陆地。现在按**族**过滤：`sameFamily(t) = (t.terrain === 'water') === isWater`，角点只收同族邻居等权、格心只按同族邻居均分。过滤依据是**成员自己的属性**、与「谁在问」无关，所以同族地块之间仍然完全一致（无缝这条硬约束不破）；陆地网格与水网格本来就是两套几何、不共享顶点，岸线因此是一条真实的分界。实测 804 个水陆交界角、逐点与「纯陆地三格平均」对拍，**最大偏差 0**（对照组：把水格一起等权混进去，颜色差 ≥ 0.01 的角占 804/804，最大 0.354 —— 证明这条断言不是空转）。
- **「沿岸度 → foam」染色已删除（v2.6）**：族隔离只管住了「角点混色」，地表层里还留着**第二条**染色路径 —— 按「该格有几个水邻居」给陆地顶点掺 `water.foam`（`SHORE_LIGHTEN 0.16`）。它就是「陆地基本色发蓝」的直接来源，且违反「水色只能由画面深度表达」这条原则。现已连 `tile.shore` 字段、常量、累加器与 HUD 的「沿岸度」一行一起删除，并加了一条「死字段不得复现」的断言（实测 0 格仍带该字段）。⚠ 查这类问题时不要只查混色公式，要问「**还有没有第二条染色路径**」。
- **蜡笔描边（按结构/用途分级）**：只有**跨结构类**的边才画墨线 —— 陆|水、陆|岩、岩|水、城|其他 收边；草|林、草|田、草|花 之间不描边，改由「混色 + 边界过渡装饰簇」咬合（分级表在 `palette.outlineClass`）。早先的规则是「任一侧地形标记为 outlined 就画」，于是农田与花田的每条边都有墨线，整张图被切成一格一格。一条边只画**一条连续笔触**：沿边取一维平滑噪声，宽度、横向游走、浓淡都在**相邻段共享的站点**上取值，因此中心线连续、不会错开成两条断线；再叠一张「边缘毛糙 + 颗粒 + 孔洞」的笔触贴图，因此不是等宽 CAD 线（`breakRate` 保留为「断笔」选项，默认 0＝一笔到底）。
- **暖调高饱和配色**：橄榄绿草地、赭黄条纹农田、紫红花田、灰褐岩壁、砖红/紫顶小屋、橙红秋树、淡青水面、柔和天空渐变。
- **成片而非碎格**：噪声采样尺度放大到 5~9 格，再做两遍类别多数投票，区域因此连成整片；所有地表贴图都按可平铺方式生成（周期噪声 + 笔画环绕补画），不会在大尺度上留下网格接缝。
- **生态植被**：绿冠阔叶 / 秋色阔叶 / 针叶 / 灌木 / 花丛 / 作物 / 岩石七类，森林成簇、农田按行（行距由贴图周期推导，两者不会错位）、全部避开路面。
- **贴地云影（v1.6 改为贴合地形）**：以前整片「云雾」压在地表之上会让画面雾蒙蒙，于是默认关掉云雾、改用云影。但 v1.5 的云影是**比地图大 2.7 倍、悬在峰顶之上的水平面片**（`y = maxRise + 0.6`），压低到贴地视角时它会横贯整个画面、盖住树与屋，纹理被极度拉伸后排成一排排细线（用户反馈的「很多粘连的线」）。现在改成**贴合地形的网格**（逐顶点 `heightAt + lift`，约 90×64 段），贴图提到 512 并开各向异性过滤；两个环境层默认都不开，需要时在 HUD 里各自挑选。详见下文「v1.6」。
- **扁带的横向贴地**：路面 / 车辙 / 路缘 / 钢轨 / 蜡笔边线 / 枕木本质都是「沿折线铺的扁带」，过去三列顶点**共用采样点那一个高度**，只有中心线跟着地形走；8.8 单位宽的御道横跨陡坡时必然一边浮起、一边插进地形，掠射视角下就是悬空的细线。现在 `Ribbon.pushBand` / `pushCrossBar` 支持 `ground + maxDrop`：左右两列（枕木是四个角）各自落到地形上，并钳制在中心线 ±`road.conformDrop` 内；桥面 / 栈桥面 / 洞内路面传 `null` 保持原高度，不会被摊到谷底。
- **沙盘感**：地图坐在一块底座上，保留方向光与阴影（用户要求保持立体感，后续还要做日夜与季节）。

## 山体（微缩沙盘）

地表网格每格只有「格心 + 中环 + 6 个共享角点」13 个顶点 —— 这是「地块之间没有台阶」的前提，也因此**无论把山抬多高，形状永远是缓丘**。所以山体是**叠加在地表之上的独立几何**。

**形状只有一个真源：`world/mountain-field.js`。** 渲染层（`render/mountain-layer.js`）只做三件事：采样、建网格、上色 —— 它里面没有任何「每格的形状」概念。

```
H(x, z) = 包络 × 轮廓掩码 × ( 岩台 + (1 − 岩台) × 脊带 × 峰高 × 脊网 )
```

### v2 为什么换掉「椭球穹丘」（2026-09-18）

v1.9 的写法是 `dome = 1 − ru² − rv²`（整簇一个椭球穹丘）+ 脊状噪声**向下雕刻**。实拍反馈「山脉太平了、没有起伏」，量化之后根因有三条：

| 实测指标 | v1.9（椭球） | v2（窄脊带） | 参考图 |
| --- | --- | --- | --- |
| 体量比（峰高 / 半高半径） | **0.14** | 2.51 | ≈ 1.0 |
| 脊状噪声对造型的贡献 | **±13%**（`1 − 0.5×(1 − rg)` 只在 0.67~0.93 之间飘） | 主要项 | — |
| 单格孤峰明暗图 | 一圈同心环（光滑圆包） | 6~9 条放射脊 / 冲沟 | 放射沟 |
| 最大山簇尺寸 / 峰高 | 34.8 × 18.2 格 / 1.30 格 | 14.7 × 5.3 格 / 1.43 格 | 2~4 格宽窄带 |

- **椭球的几何方向就是错的**：`1 − ru² − rv²` 是「倒扣碗」（顶平、根陡），山的横剖面必须是**凹形**（顶陡、根宽）。所以换成 `belt = (1 − nd)^flankExp`，`flankExp 1.45 > 1` 才有裙摆。
- **它只在几何中心有一个极大值**，脊状噪声又只能做减法（`shape ≤ dome`）⇒ 每簇最多一个峰，永远长不出「峰—鞍—峰」。
- **噪声的实测值域不是满量程**：`ridgedPerlin2` 的 min/p05/中位/p95 = 0.11 / 0.34 / 0.62 / 0.87。直接当系数用只剩 ±13% 起伏；v2 用 `crestSharp 3.0` 把它拉到有效量程（中位压到 ~0.24、保住脊顶 1.0）。

### v2 的形状（逐项）

- **脊带**：山体是一条沿簇主轴延伸、横向**蜿蜒**（`beltWander`）的窄带，半宽 `beltHalfWidth: 1.15` 格（≈2.3 格宽，正好落在「单格地块也能单独撑起一座山」的量级）。半宽沿走向抖动 `beltWidthJitter`，两端收口 `beltEndTaper`。主轴由簇内格心 PCA 给出（单格簇退化为 +X，确定性）。
- **峰高沿走向调制**（`summitScale 2.2` / `summitFloor 0.32`）：这是「峰—鞍—峰」的来源，也是参考图里「基安蒂山脉」那种连绵脉的关键。实测最大簇：脊上 5 个局部极大、最深鞍部 2% 峰高。
- **脊网**（`ridgedPerlin2` + `crestStretch 2.4` 沿走向拉伸）：`|n| → 0` 的等值线是**曲线**，天然没有格点偏好；它是「脊线与冲沟落在哪」的判据。
- **放射脊 / 冲沟**：把「到脊线的横向距离」按噪声调制（`spurAmp 0.46`）⇒ 等值线成**星形**，脊与沟沿坡面放射。单格 / 短簇用**角向**取样 —— 在噪声空间里 `perlin(cosθ·k, sinθ·k)` 天然以 2π 为周期，所以不需要另写一个周期噪声函数。实测最高孤峰：放射脊 17 条、环向起伏 63%。
- **低岩台**（`baseLevel 0.20`）：整簇的底盘，保证山格本身是一块起伏的岩石高地（而不是悬空的脊带），同时又足够低 —— 实测最大簇格心场值 31%~82% 峰高、**峰:底 = 3.23**（v1.9 的椭球把整簇抬到 63% 峰高，这就是「平台感」的来源）。
- **轮廓掩码**以「到簇边界折线的**有符号**距离」收脚：内侧 `taperInner: 0.45` 格开始降、外侧 `taperOuter: 0.30` 格归零，再叠一层低频噪声抖动（`outlineWobble: 0.22` × `outlineWobbleScale: 1.6`）。**外侧 > 0 就是「允许山脚漫出簇边界」** —— 接触线因此是一条噪声曲线，而不是格边折线。实测 282 / 282 条簇边界边外侧仍是山体。
- **逐簇归一化**：`body = 脊带 × 峰高 × 脊网` 是三个各自 ≤1 的噪声相乘，极大值不落在同一点 ⇒ 裸场峰顶只有包络的 0.6~0.8（v1.9 文档里那句「实测峰值只到包络的约 0.74」就是这么来的，雪线因此吊在够不着的地方）。v2 在编译时量一次实测峰顶、把整簇场缩放回去，于是 **`peakHeight` 就是「这座山有多高」**，与任何噪声参数解耦（实测 18 簇：maxField 恒等于 `amp`）。

### 山脉的分布（一起收窄成窄带）

「起伏通道」的噪声从 `fbm2` 换成了 `ridgedPerlin2`（`src/world/hex-world.js`）。原因是实测出来的：

- 山格 = 「起伏通道的高分位集合」，所以它的**形状**完全由这条通道的极大值长什么样决定；
- `fbm2` 的极大值是**团块** —— 只调 `ridgeShare`（0.10 → 0.42）平均簇宽都从 3.6 涨到 6.1 格，长宽比一直在 1.3~2.0，**永远收不出窄带**；
- `ridged` 把 `|n| → 0` 的等值线翻成极大值，那是**曲线** —— 取 top 22% 得到的就是蜿蜒窄脊带。

实测（`ridgeShare 0.22` / `scale 11`）：山脉占陆地 **16.8%**（v1.9 是 32.2%）、18 簇、平均簇宽 2.9 格、最长 14.7 格，6 片孤峰。连带影响：河源 7 条、隧道 7 格、桥 14 格，都在。

### 演示用的大片山脉（`relief.demoMassif`）

给策划看「一大片山脉长什么样」用的**手工块**：偏移坐标（odd-r）一块 4×5，默认贴地图左下角
（`col −11, row 5`，世界中心约 (−351, 231)，离城 4 格）。它放在「按排名切分」之后 ⇒ 手工优先于
比例生成 —— `ridgeShare` 不保证任何具体位置，而「左下角要有一大片」只能显式指定。

- 只跳过**地图边界水格**（`border`，海岛轮廓外圈），块内普通水格改成山格 ⇒ 块内永远是一整片、
  **连成 1 簇**（实测 20 格全成山、1 簇，其中 18 格是新改建的，含 1 格沿海水格 → 做出「山体探进海里」
  的样子，正是参考图里那种海岸山体）。
- 它**不计入**目标份额，也在「生成的山脉分布是窄带」这条断言里被剔除 —— 否则那条断言会退化成
  「演示块有多大」的间接度量。
- 配套两条形状规则（对**生成出来的**山脉同样生效）：
  · `beltWidthGrow 0.85`：`hw = max(beltHalfWidth·hexSize, 0.85 × extV)`。脊带随簇宽增长，否则
    20 格的大块会读成「一条薄脊 + 一大片岩台」。单格簇 extV ≈ 1 格 ⇒ 增长项打不过下界，
    **单格山形态完全不变**。
  · `peakHeightGrow 0.32`：`amp ×= 1 + 0.32 × clamp((簇格数 − 5) / 12, 0, 1)`。簇越大峰越高 ——
    否则「10 格宽、1.3 格高」还是个缓丘（实测体量比 0.79）。只对 **≥ 6 格**的簇生效。
- 实测：演示簇 22 格 / 峰高 **1.66 格** / 脊带半宽 **4.21 格**（下界 1.15）/ heightGrow 1.32。

### 山谷：山脊对面的一条沟（`mountains.valley`）

参考图里很常见的构造：一条脊线旁边跟着一条同向的沟。做法是在**脊带局部坐标**里，沿一条与脊线
平行的中心线挖高斯槽（谷底 = 当地高度 × (1 − 谷深)）：

- **单格与连续地块都可能出现**，是否出现由「簇号 + 种子」的哈希决定（`rate 0.55`）⇒ 确定性、可复现
  （实测 9 / 17 簇带谷：单格 2 + 连续 7）；
  ⚠ 判定必须是**三态**：`'on'` / `'off'` 是策划的显式决定，`'auto'` 与缺省是「没指定」、要交回 `rate` 判定。
  曾经把 `plan.valley === 'off' ? false : (plan.valley === 'on')` 的结果当三态用 —— `'auto'` 也算出 `false`，
  与「显式关闭」不可区分，`rate` 分支永远走不到，于是**默认世界 17 簇一个谷都没有**。
  另一条：`stableHash` 必须是**数字**（`Rng.hashInt` 内部是 `x | 0`，传字符串会被压成 0，
  所有簇共用一个哈希 ⇒ `rate` 退化成「全有或全无」）。
- 单格簇的脊线退化成一个点（`endCore = 0`），所以另给谷一个朝向 `valleyAz`（也由哈希给出）⇒
  **单格地块的山脉 / 山峰可以是不对称的**（「与山脊相对的山谷」）；
- ⚠ 刻在**合成后的高度**上而不是只刻脊带：只刻脊带时谷在坡脚几乎看不见，实拍读不出「沟」；
- 验收必须用 **A/B 对拍**（关掉 `rate` 再编译一次、在**同一坐标**上比）：单点采样分不出「谷」与
  「脊线自身的蜿蜒」—— 实测无谷的簇两侧就能差 2.04 倍。实测带谷簇的谷心线被压低 **30%+（最强 51%）**，
  而**镜像侧只差 6%**（全部来自归一化系数的微差）。

### 演示块与山谷的实测（v2.1）

| 项目 | 数值 |
| --- | --- |
| 演示大片山脉 | offset(−11,5) 4×5 → 20 格全成山、**连成 1 簇**（新改建 18 格）；簇 22 格 / 峰高 **1.81 格（39.81 单位）** / 脊带半宽 4.21 格 |
| 山谷 | **9 / 17 簇带谷（单格 2 + 连续 7）**；谷心线压低 30%+（最强 51%）、镜像侧偏差 ≤ 6% |
| 穿山隘口（`river.demoWaterway`） | 318 个河段采样点：**202 段 `mountainPass` + 116 段 `auto`**；受影响采样点 200 个、穿越处**满额侵蚀 1.000**（`pass.depth = 1.00`）；**171 个河道横断面全部开通**（修前 169 个被岩石压住） |
| 生成山脉分布（已剔除演示块） | 16 簇 / 平均簇宽 3.1 格 / 最长 14.7 格 / 占陆地 **17.1%** |

以下三条是**设计红线**，换实现也要守住：

- **红线①：高度只有一个来源，且只依赖 (x, z)**。同一世界坐标只有一个值 ⇒ 相邻格、跨格、跨簇一律同高，裂缝在机制上不可能出现。实测：3210 个采样点场值恒 ≥ 0、低于地表 0 个；**0 个重复位置顶点**。
- **红线②：山脚不低于地表**（表面高度一律走 `MountainField.surfaceAt` = `max(地表, 场按水流侵蚀权重插值回地表)`）。否则丘陵微起伏与河床浅切槽会让地形从山脚里穿出来，落地墙还会变成朝上的反墙。⚠ 这里**不能**写成 `max(场, 地表)`：那是「山壳盖住一切」，会把穿山河段已经开好的切口重新盖回去（见下文「河流」与方案 §15.21）。实测各 LOD 级表面超差 0、墙脚超差 0、悬空自由边 0、退化面 0。
- **红线③：壳体只落在轮廓线上**。「场值归零的那条等值线」就是山脚接触线，沿线立竖直墙落到地表。因此**自由边只剩贴地的墙底边，空中没有任何敞开的边** —— 这就是「表面闭合」的全部含义。实测各级翻面 0、索引越界 0、悬空自由边 0。

拓扑与建网格：

- **共享三角格网格器**：整簇按 `step = hexSize / detail` 铺一张网格、逐点取样场，相邻格**复用同一批格点**，所以格子感从结构上消失。同一簇内按位置焊接（1/512 单位量化）。表面三角按 `ny ≥ 0` 定向（高度场不会悬垂，判据只有这一条）；只有竖直落地墙用「背离格心」的判据。
- **孤峰（单格成山体）**：6 邻无山的山格标记 `isLone`，峰高乘 `loneScale: 0.85`（实测孤峰最高 26.87 vs 最大簇 31.38 单位）。旧版那条「孤立山点软化」循环已删除，现在实测 63 格里 6 片孤峰。
- **顶点色**（`palette.mountain` + `rockBands`）：岩带按归一化高度 `y / refH` 做两段 smoothstep 混合（带抖动）；**冲沟压暗** `creviceShade` 用采样网格的**凹凸**判定（某点比 4 邻平均低就是沟）—— 不重算任何噪声，天然跟着形状改动走；再叠**雪带** —— 雪线 = `refH × snowRatio`（0.66），沿主轴拉伸的噪声让雪成为**顺坡的条带（残雪）**而不是一圈整齐的环，`snowFade` 控制渐变宽度。最后**山脚往「脚下地块的地表色」过渡**（`footBlend`）。实测 19 / 63 格在雪线以上、最细一级近雪色顶点 1633 个。
  ⚠ `refH` 取**本簇实测最高峰**（归一化之后它恒等于 `amp`），所以每条山脉都有自己的雪顶。
- **山体占位**由 `MountainLayer.occupancy(world)` 统一对外提供：植被、房屋都问它一次，才不会有杉树从山体里穿出来；山格只长岩石，且按 `occ.thickness(x, z) < ridgeScreeClearance` 落在**山脚之外**。实测落在山体格内的道具 0 个。
- **不做轮廓线**：山体不加墨线描边（按用户决定），剪影靠分带顶点色的块面感 —— 山体是独立网格、不与地表共享顶点，加描边一定会在地表上多出一圈闭合线。
- **场只编译一次**：`plan()`（渲染）、`occupancy()`（植被 / 村落各一次）原本各自编译一遍场 —— 实测同一份世界被编译 **4 次**、白花约 76ms。现在 `MountainField.compile` 按 world 用 WeakMap 记忆化；**就地改配置数值时要显式调 `clearCache()`**。

## 山体 LOD（按缩放切采样密度）

高度场是 `(x, z)` 的**纯函数**，所以「同一片山体用不同间距采样」得到的一定是同一个地形的不同精度版本 —— 这是 LOD 能做得这么轻的根本原因。

- **块 = 山簇**。相邻簇即使级别不同也不会裂：簇边界处场值恰好归零、落地墙把边界压到地面，接缝处两者都在地面上 ⇒ **结构上没有 T 型缝**，不需要缝合带 / 裙边。
- **判据用屏幕像素密度，不是裸距离**：`detail ≥ hexSize / (targetPxPerStep × 每像素世界单位)`。本项目有正交 / 透视两种相机（**默认档是低透视**，由按钮切正交；正交没有距离衰减），拿裸距离做判据在正交下是错的。
- **滞回阈值取相邻两级的几何平均**（对数中点）`√(当前 × 邻级)`：变细要求需求超过 `√(dc×df)×(1+h)`，变粗要求低于 `√(dc×dk)÷(1+h)`。⚠ 早期写法拿「当前级自己的 detail × (1+h)」当阈值，它落在自然切换点的**错误一侧**，等于滞回完全不生效（实测两个方向都会跳）。
- 相机静止时整个 `update` 直接短路；最短重算间隔 `updateInterval 0.12s`。
- 级别表 `lod.details: [12, 8, 5, 3]`（细 → 粗，步长 1.83 / 2.75 / 4.40 / 7.33 单位）。`lod.enabled: false` 时锁在 `details[0]`（最细一级）。

实测（本图 17 簇，含演示块）：

| 级别 | 步长 | 三角形 | 块数 |
| --- | --- | --- | --- |
| 12 | 1.83 | 70576 | 17 |
| 8 | 2.75 | 32378 | 17 |
| 5 | 4.40 | 13402 | 17 |
| 3 | 7.33 | 5216 | 17 |

- **性能**：四级全部预建（一次性约 3.5s，软件渲染且受机器负载影响大），默认视角（距离 1250；该距离下低透视与正交的像素密度一致，见 `applyRig` 的 `ortho.zoom` 对齐）判据算出需求 ≈ 3，实际渲染 **13402 / 70576 = 19.0%** 的三角形。
- **它省的是 GPU 顶点处理，不是建造耗时**：要预建所有级别，建造时间是各级之和（几何级数，粗级几乎不要钱）。当前地图规模下这不是瓶颈（分层耗时见下），**真正有收益的场合是编辑器产出的大地图**（几千个山格时总面数上 50 万、阴影通道也变重）。所以默认开、可一键关。
- 已知边界：块粒度是「一个山簇」，所以一条超长山脉内部无法混级。若编辑器将来产出超长单簇，再按 ~6 格分段 + 一圈裙边处理（方案 §15.17）。

分层建造耗时（软件渲染 / Swiftshader，535 格）：地形 380ms / **山体 3457ms** / **水面 79**（河 / 泉 / 海已在同一份几何里）/ 描边 40 / 道路 54 / 植被 45 / 村落 17 / 城市 15。

## 水面与「画面深度过渡」

河 / 湖 / 海现在是**同一套水**（同一份几何 + 一份材质，v2.8）。但「高度」这件事在阶段二变了：**只有海面还是一个平面**（`water.level = 0`），**河面按地块档位沿程阶梯下降**（源 1.406 → 海口 0.000），**泉湖各自的碗有一条自己的水面**。无论哪一类，水面都压在一张被切下去的水下地表之上 —— 这两者含义完全不同，必须分开建：

- **水下地表（bed）**：水格的地表由 `world.heightAt` 给出，是两项相乘 ——
  · **敞水深度场（v2.6 改成连续离岸距离）**：`depthShallow 0.016` → `depthDeep 0.09`，跨 `depthRamp 2.5` 个**格心间距**过渡；自变量是**到最近陆地格六边形的连续距离**（不是「离岸多少格」这个整数）。为什么必须连续：水深驱动下面的画面深度过渡，而旧写法按整数格距分级 ⇒ **全图只有 3 个水深档位**（0.352 / 1.003 / 1.654），相邻水格最多差 40% 的过渡量，过渡把它们原样放大成**一块块硬边多边形**（用户截图里的色块）。现在在格心处与旧公式**逐值相同**（ring-k 的比值仍是 (k−1)/2.5），但点与点之间连续：实测「到最近陆地格距离 × 岸坡因子」的暴力对拍 1292 点**最大误差 2.2e-16**，同一离岸档位内水深极差 0.297（旧版 0），共享边跨格差 0.0010。
    梯度用 smoothstep 收放（`u²(3−2u)`）：在「贴岸浅滩 → 斜坡」与「斜坡 → 深水」两处斜率都归零，于是**深度的一阶导也连续**，水面上不会留下一条条等深线。平台（深度不变）只允许出现在贴岸那一圈（离岸 ≤ 内切圆半径）—— 实测 18012 个采样点里 1642 段平台**全部**贴岸。
  · **岸坡因子**：0 在岸线上、`shoreRamp 0.7` 格内升到 1；只在**临陆的格边**上量距离。实测：水/水共享边 0 处非零，岸线处为 0（唯一例外是 **5 处河口**，那里被河的浅切槽切下去 2.20 —— 本来就该凹）。
  · ⚠ **地图外按开阔水域算**（v2.6）：旧版把「取不到邻格」也算成岸线，于是海底在沙盘边缘抬回水面 —— 外缘一圈水莫名变浅发亮、水与海底还共面。实测外缘 182 个格边采样点高度**全部 < 0**（最高 −0.40；修前 182 个全为 0）。
  · ⚠ **海底不加格内微起伏**（v2.6）：`innerRelief × maxRise` 实测 1.25 单位，而水深只有 0.35 ~ 1.48 —— 旧版给水下地表也加这层起伏，**272 / 948 个中环顶点被抬到水面之上**（最高 +0.997），海底在整片水面上穿出一圈圈硬边格子斑块，深度过渡也读到一堆逐格噪声。现在这层起伏**只给陆地**，海底形状交给连续距离场（实测水下地表 2054 个顶点最高 y = 0，高于水面的 0 个）。
  ⚠ 更早的「每格一个碗」写法（v1.9 之前）：碗底的 0 落在该格**全部六个角点**上，连深海中央也是 0 ⇒ 整片海变成一格一个洼的六边形凹凸纹。角点只有一种情况该是 0：这个角点**真的踩在岸线上**（三格里有陆地）。
- **水面（v2.8 阶段二：海面仍是平面，河面沿程阶梯下降，泉湖各用自己的水位）**：海面是那个严格水平面，高度只由 `config.water.level` 决定、与地形完全解耦（实测 3002 个海面顶点全部落在 `y = 0`）；河面按**地块档位**取值、沿程**单调不升**、入海河河口钉回海面；泉湖各自的碗面取自己的水位（实测各水体水位 0.00 ~ 1.41）。整份统一水面实测 **12934 个顶点、高度 y ∈ [0, 1.4064]、全图落差 1.406**。几何本身是静的，「波动」交给 GPU 顶点位移（只作用于开阔海面，幅度 1.540 单位 = 0.070 格）。**海面顶点色是一个常数**（实测 12934 个顶点里排除河 / 泉与河口湿岸共 10034 个后**全等**；旧版把 ±30% 的大尺度手绘斑驳也铺在水上，读成「水面深浅不匀的斑」）—— 深浅只由下面的深度过渡给出。
- **画面深度过渡（`render/water-depth.js`）**：水面网格本身是平面，它凭什么读出「水有多深」？靠一张**半分辨率的深度预通道**（只画地表 + 山体，水面自己不在里面 —— 否则「地表深度」恒等于水面深度、差值为 0），水面材质在片元里比较「本片元深度」与「该像素处地表深度」：

  ```
  waterWorld = reconstructWorld(gl_FragCoord.xy, gl_FragCoord.z)
  bedWorldY  = bilinear(reconstructWorld(depthTexel_i).y)
  dz = max(0, waterWorld.y − bedWorldY)
  t  = smoothstep(clamp(dz / depthFade, 0, 1))
  颜色 = mix(palette.water.shallow, palette.water.deep, t)
  不透明度 = mix(depthAlphaMin, 1, t)
  ```

  差 ≈ 0（岸边、水线）⇒ 水面几乎透明、透出水下地表，岸线自然收边；差 ≥ `depthFade` ⇒ 不透明并转向深水色。**「水」与「地」之间因此不再是硬边，而是一段由真实几何竖直水深驱动的过渡**；海 / 河 / 泉三种水面材质共用这三段共享 GLSL（`waterWorldAtDepth` / `waterBedYBilinear` / `waterDepthRatio`）与同一份 uniform 对象，`water-depth.js` **不再拥有任何颜色**（v2.7）。
  · **世界 Y 差是唯一深度语义**：水面片元与深度图 texel 都用逆投影矩阵和相机世界矩阵重建世界坐标，直接计算 `waterWorld.y − bedWorld.y`。不再依赖视空间差、近远平面分支或 `uInvViewCos` 一类俯角补偿；同一片床面在正交 / 透视、旋转和缩放下仍由同一组世界 Y 值决定 `t`。
  · ⚠ **不要额外叠加视角相关的不透明度**：水深已由真实世界 Y 差决定。以前按 Fresnel 直觉加入「视线越平越不透明」的项，A/B 对拍没有形成独立、稳定的视觉收益，且会重新把相机姿态耦合进水色；该类路径保持删除。
  · ⚠ **不要让 `t` 在屏幕上分块**：深度纹理在 WebGL 里只支持 NEAREST 采样，而预通道是半分辨率。当前片元着色器保留手动双线性：取相邻 4 个 texel，分别重建各自的 `worldY` 后再按双线性权重插值；不能先插值非线性的窗口深度。背景 texel（`depth ≥ 0.9999`）权重置零并重新归一化，四个都无效时回退最近邻；`config.water.depthFilter` 留 `'nearest'` 作对照档。实测双线性行奇偶差为 **0.18 / 0.03**，切回 nearest 为 **3.72**。采样步长必须使用深度图自身尺寸（`uDepthStep = 1/rt.width, 1/rt.height`），不能拿主画面的 `uDepthTexel` 顶替。
  · UV 换算要用**绘制缓冲尺寸**（`gl_FragCoord` 的单位是它，不是 CSS 像素），并且本模块比 `sceneKit.resize()` 先创建，所以尺寸**每帧自检**（否则会拿到 canvas 默认的 300×150，建出一张 150×75 的深度图）。
  · 深度贴图用 24 位（`UnsignedInt`）：16 位在 `far ≈ 4000` 时每级 ≈ 0.06 单位，而过渡区间只有 2 单位，会看出台阶。
  · **实测对拍**（俯视相机、射线竖直 ⇒ 深度图读到的就是该水格的水下地表）：最深的水格真实水深 1.482 / 量到 1.46~1.52；浅滩 0.352 / 量到 0.28~0.36（半分辨率 + 手动双线性采样，逐次有 ±0.06 抖动）；深浅在同一套尺度下被区分开。⚠ 通道读的是**渲染网格**的水下地表，所以「把海底形状做对」比「把公式写对」更重要 —— 下一条正是踩在这里。
- **手绘卡通水体材质（`render/water-material.js`，v2.7 重写）**：海 / 河 / 泉**共用一份自写 GLSL**，只差 profile（色板、波速、泡沫带宽、河口混合），不再是「往 `MeshStandardMaterial` 里注入」。旧写法有两个硬伤，都是量出来的：① 注入点在 `#include <color_fragment>`，而 r147 里 `<map_fragment>`（贴图）与 `<color_fragment>`（顶点色）**都在它之前** —— 一句赋值把两者整体覆盖，实测把 `map` 置空、`vertexColors` 关掉，画面差 **Δ0.000**（水纹贴图与逐格顶点色对水色毫无贡献，河流的顺流 UV 也因此在空转）；② 波浪只在**顶点**位移，而水面每格只有 13 个顶点（间距 ≈ 11 单位）< 主波长 16 单位，属于严重欠采样，且振幅是唯一**不乘 hexSize** 的水体参数（0.045 当世界单位用 ＝ 格距的 0.2 %），实测把振幅 ×20 才让画面变化的水像素从 0.97 % 跳到 59.37 % —— 这就是「看不到波浪」。
  现在：**波浪全部做在片元**（世界 XZ 的 sum-of-sines + 解析偏导得到法线，与网格密度、相机分辨率都无关，也不需要顶点位移 —— 水面与陆地共面，向上位移会顶到陆地）；**手绘卡通光照**把 `dot(N, L)` 量化成 2~3 档软边色带（cel shading）+ 阶梯式高光，与全图的蜡笔 / 墨线风格统一；**泡沫统一收进水面 shader**，判据换成**真实水深**（`dz < foamWidth`）而不是「波峰高度」，海 / 河 / 湖 / 河岸同一套，噪声破边 + 沿水线脉动（ink 层那条静态泡沫通道已删除）；水色 = 浅 → 深（由深度过渡的真实世界 Y 差驱动）× 波峰提亮 / 波谷压深 × 水纹贴图（**真正参与**颜色，权重可控）。光照 / 雾 / 输出编码 / 投影全部沿用 three 的标准 chunk（`fog_pars_vertex` / `fog_vertex` / `encodings_fragment` / `fog_fragment`），不用全局 ACES。三类水面**按引用共用**同一批 uniform（共享深度 + 共享光照），实测 `stats().sharedDepth / sharedLight` 均为 true。颜色约定与全图一致：palette 的十六进制值**按原值当线性值用**（r147 默认 legacy 颜色管理），所以**不要**在材质里单独 `convertSRGBToLinear`，否则水面比陆地暗一档。
- **河口（v2.7）**：河流末端不再是「硬边接上海面」。每个河面顶点带一个 `aMouth` 因子（0 源头 → 1 河口），片元据此把河色**混向当前海色**（`uMouthShallow / uMouthDeep` 与海面**共用同一份对象**，环境切换时河口跟着变）并叠加河口泡沫；几何上还在入海口生成 2~3 条**分流带**（三角洲，起点取自干流中心线上的同一点，不露缝），以及一圈**冲积带**色（`alluvial(x, z)` 是世界坐标连续函数，与 `floodplain` 同一技术，**只做表现**：不改地块分类、不改高度、不改寻路）。海 / 湖 / 泉的 `aMouth` 恒为 0。
- **表现/逻辑边界**：`water-depth.js` 只做两件事 —— 跑深度预通道、把共享 uniform 与 GLSL（`waterWorldAtDepth` / `waterBedYBilinear` / `waterDepthRatio`）提供出去；它**不再拥有任何颜色**（旧版另存了一份 `uDepthShallow / uDepthDeep`，同一份 palette 被读两遍、混两遍，水色被往「淡」里拉了两次 —— 这是水色偏白的成因之一）。海、河、泉共用 `Rivers.waterLevel(size)`；河流继续使用 Ribbon 和逐点 `halfW`、`channelOffset`。河口不再用 `mouthOffset`（只抬高末端一个渲染采样点 ＝ 0.05 % 格距，看不见，已删除），改成**渲染偏置 `renderBias`（只加在渲染层，逻辑水位不变）+ `mouthBlend`**。岸带继续由河流影响场、湿润 / 洪泛色带与共享泡沫共同表现。
- **河流规范（v2.7）**：所有**主河**必须有源头与入海口 —— 入海口判据是「接进**大片连续纯水**」（`world/hex-world.js` 对水格做六邻域连通域，面积 ≥ `config.water.seaMinBodyTiles` 才算「海」，默认 12）；实测默认世界 10 个连通域（最大 140 格），4 条主河的入海口**全部**落在 140 格的那片纯水里。**支流不要求入海**：改为报告汇流点 `confluence`，不再给假 `mouth`；走不到水又没有汇流的轨迹按 `river.onDeadEnd = 'drop'` **丢弃**（默认世界丢弃 1 条），而不是画一条停在田中央的河。
- **顺带删掉了一处重复逻辑**：顶点色里原来还有一条「浅水带」（按 `distToLand / 3.2` 把水面朝 `water.shallow / water.foam` 提亮）。它看上去也在做「近岸浅、远处深」，但**必然是一格一格的** —— 同一个水格上，格心顶点的权重是「本格 + 6 邻」、角点顶点是「压在角上的 3 格」，两套权重算出的 `distToLand` 平均值不同，每个六边形内部都会有一圈色阶，实拍就是水面上的**蜂窝纹**。水深现在由几何唯一给出，这条就删了。

## 河源水体（泉眼 / 小湖）

河流原本是一条**和别处同宽的水带凭空开始**。v2.5 在河源处补上了「源头」：`world/river-builder.js` 在河源顶点附近选一格刻一个**碗**（由 `world.heightAt` 叠加 `tile.springRefs`），一片水面覆盖上去，于是河源变成一个泉眼或小湖。

- **凭什么在数据层就能成立**：碗只**往下切**（`h = min(h, -depth)`），所以「水面永远在最上层」这条不变量自动成立。**水位不再取全图统一值**（v2.8 阶段二）：每条水体有自己的 `level = min(该格档位, 碗沿自然地面最低值)` —— 碗沿按 `1.02 × radius` 采 8 个方位（那里下切量正好归零，量到的是自然地面）取最低，于是「水面不高于碗沿任何一点」是结构保证，丘陵上的湖不会浮在碗沿之上。实测各水体水位 **0.00 / 1.32 / 1.35 / 1.41**（3 湖 1 泉）。河源处的河面另有一步「源头水位对齐」把两者接上（见下文「河流」）。
- **三条硬约束**（都来自已有不变量）：① 碗心从河源顶点朝「最开阔的那一格」的格心退 `pullback`（不落在顶点上，也不伸进山壳）；② 半径保证够不到**相邻的共享角点**（实测裕量 0.21 格），否则同一物理角点会算出两个高度 ⇒ 裂缝 —— 而且**共角的三格都要记这份下切**；③ 避开山格（会被山壳盖住）与水格（那里已经有水），共角三格都不可用就不放，计入 `springSkipped`。默认世界实测：**4 处水体（3 湖 / 1 泉）+ 2 处跳过 = 6 条河**，全部有交代。
- **碗与水片是两个尺度**（config 的 `basin` 与各形态的 `water`）：碗半径由**地表网格的分辨率**下限决定，不是美术喜好 —— 每格只有 13 个顶点（格心 + 中环 + 六角点），碗太小就一个顶点都压不到，网格解析不出盆地。实测碗半径 0.32 格时水面片外圈被地表**顶穿 33/45 条竖直射线**（解析式 `heightAt` 在那时是对的，所以只有「打到网格上」才看得见）；调到 0.72 格 × 水片比例 0.62/0.46 后**顶穿 0 条**，只有 2 条落在线上的山脚（容差内）。
- **岸线不是正圆**：`SpringLayer.shoreRadius` 用两个整周期谐波把半径扰动 ±10%（首尾闭合），否则一个正圆贴在地表网格上读起来像「画上去的圆盘」。实测每片 40 个方位的外圈半径比 0.900~1.096。
- **涟漪**：水面片是极坐标圆盘（`SEG = 40`、`RINGS = 4`），UV 的 `v` 轴 = 归一化半径，于是滚动 `offset.y` 就是一串向外扩散的涟漪，不需要写着色器。贴图 `Textures.rippleTexture` 在 `v` 上取整周期（回绕不留缝）、方位向加相位抖动（免得读成靶心），幅度刻意压到 ±13/255 ≈ 5%（第一版 7 圈 ±13% 在泉眼上就是个同心圆靶心）。水面材质进深度过渡的注入列表 ⇒ 深浅与海 / 河同一套规则。
- **「融合地块」的那一圈**：水面边缘往外 `wetBand × 碗半径` 内把地表朝 `river.bedTint` 靠拢（水面边缘最湿），再叠一点 `water.foam`；碗内的碎石与湿生灌木由 `props-layer` 撒在水片外圈（半径从 `waterRadius` 起算），道具与**道路**都按 `springAt()` 避让 —— 穿过水面的路段抬成桥面（否则路会跟着 `heightAt` 沉到水面以下，视觉上就是「路扎进湖里」）。
- ⚠ **两个渲染层的坑**（都留了回归断言）：① 一个几何里装多片圆盘时，索引必须带上每片的**起始顶点号**，漏掉就会把第 2 片之后的所有三角形接到第 1 片的顶点上（实测竖直射线只有第 1 片打得中）；② 判「水面上有没有别的面」要用**线段**到碗心的距离（道路采样步长约 7 单位，与水面片直径同量级，只看采样点会漏判）。

## 河流（权威中心线、沿程阶梯水面与穿山切口）

参考图里的河是**地块与地块之间的边界**：地块落在河的哪一侧、哪个聚落临河，都有玩法含义；穿格心的河会把地块从中间切开，地块语义就没了。所以河流严格走六边形的棱（`world/river-builder.js`）：

- **顶点图上的寻路**：把所有地块的 6 个角点按位置去重建成**顶点表**，沿棱把顶点连成图 —— 于是「走一条棱」恰好就是「跨过一条格边」，从根上避免河斜切进格内（旧版是「tile 路径 + 取中点」，一旦方向映射写错就会取到镜像的那条边，出现 3.279 格的斜向直线段）。河源取「融雪线以上最高的角点」里彼此相隔足够远的几个，实测 4 条主河全部从山地 / 丘陵出发。
- **势能场保证必定入海**：先算「每个顶点到**入海口**在图上要走几步」（BFS），再叠一层噪声；每步只能走到势能更低的邻点，而**噪声权重 `meander = 0.45 < 1`**，所以每一步势能都严格下降 —— 既不会绕圈，也不会停在田中央（旧版靠有限次「翻坎」逃洼地，是概率性的）。v2.7 起 BFS 的种子**只放「大片连续纯水」的顶点**（`isMajorSea`，见上一条），所以「入海」这件事是结构保证的，不靠末端碰巧落在哪一格；实测 4 条主河 `reachesSea` 全为 true。河会**绕开城市格**（否则水带会被城市广场模型整片盖住）。
- **汇流 = 撞上别人的顶点**：下一步落到别的河已经走过的顶点即为汇流，本河并入、下游共享同一水道，不需要第二套渲染规则。
- **河面沿程阶梯下降（v2.8 阶段二）**：河面高度不再是「全图一个水位」，而是**按所经地块类型逐级下降**（`applyLevelProfile`：逐采样点取附近地块档位的最大值 → 沿程单调不升 → 台阶摊成 `river.stepSlope` 格的短斜坡 → 河口钉回海面）。三条不变量都有断言：每条河沿程**单调不升**（实测 0 处回升）、**入海河河口钉在海面**（0.000）、全图**落差真实存在**（实测源 1.406 → 海口 0.000，落差 1.406、7 级台阶，且不超过丘陵档 2.2）。河源水体（泉 / 湖）的水面高度 = `min(所在地块档位, 碗沿自然地面最低处)`；有河源水体的河首点会被压到泉水高度再重跑单调（山里的湖比丘陵档低 ⇒ 上游平台整体落下一个档，湖与河的接缝不再有台阶）。
  旧版把水位钉在「这条棱自身的岸高」上，水面会跟着地形爬坡；中间一版给河单独取 `格距 × 0.005`（0.110），结果**河口比海面高 0.11 —— 河入海处有一个台阶**；再往后「全图一个水位」虽然消掉了那个台阶，却让**落差在数据上根本不存在**（水面落差只能靠表现层假装）。⚠ 渲染偏置也已删干净（旧版海 / 泉 / 河各一个 `renderBias` = 0 / 0.132 / 0.264 单位，它就是河口台阶与「方头」的直接来源），波动改由材质在 GPU 上做顶点位移。
- **分层河宽**：主河保持 `river.width = 0.16`（× 格距）即半宽 3.52 单位；支流从 `tributary.widthSource = 0.075` 连续增至汇入口的 `widthMouth = 0.11`。每个采样点的 `halfW` 是唯一权威，河面、河槽、湿岸、漫滩、植被避让和山体侵蚀都按最近河段插值后的同一宽度计算，避免出现水面与河床脱节。
- **浅切槽 + 河岸走廊**：水面要看得见，就必须让河线附近的地表低于水面。`riverProfileAt(x, z)` 是**唯一**的一处最近段查询（一次返回「局部河面 + 到下切的量 + 走廊权重」），`world.heightAt` 读它做两件事：
  · **槽内**（`off > 0`）按「到河线距离」下切 `channel.depth × (1 − r²)`（`r = d /（半宽 × channel.widen）`），ceiling 用**局部河面**（`level − HeightField.CEILING_EPS`）而不是全图水位 —— 否则「下游又碰到丘陵」时丘陵会盖住水面；
  · **槽外一圈**（河岸台地，`river.corridor.band = 0.6` 格）把地表朝该处河面收，于是丘陵的波谷不会让水带悬空。走廊权重向外平滑衰减到 0，**远处的地形一根都不受影响**（断言：走廊 / 丘陵带之外的平原格心非平数 = 0）。
  实测：**154 个临河角点**被切到水面之下、**98 条临河边中段**同样低于水面（角点只覆盖格边两端，中段由中环顶点承载）、横剖面 79/79 在河线处低于水面、75/75 在半宽处仍在水下、28/28 河岸台地地面不高于该处水面；**切槽不侵入格心**（临河平原格心最大高度 0.000000）；`water-bed` 在岸线处严格为 0（例外只有河口切槽与河岸台地，且都不得高于该处水面）。地表高度只有一个入口 `world.heightAt`，切槽叠在里面，因此共享角点天然一致、相邻地块不会裂缝。
  ⚠ 岸线处两者**分开对待**：槽内下切**不受门控**（河槽必须一路切到海岸线，否则河口附近的水面会被没切下去的地面埋掉）；槽外台地**受门控**（乘 `edgeFade(waterEdges)`，否则河比海面高的那一段会把水下地表在岸线处抬起来，「水陆齐平」被打破）。
  ⚠ 判据必须是「**本格临河 或 本格是临河格的邻居**」（`riverNear`）：格边角点由三个格共享，只按本格判定会在角点上漏切，河面拐角会被顶出一条薄墙。
  ⚠ 采样点要从**角点朝格心**退 6%（≈1.3 单位）：退多了会走出槽（退 6% 时离格边还有 1.1 单位，而槽半宽 4.4 单位）；方向也不能反 —— 从格心往角点退 6% 是退到离角点 20.7 单位的地方，早就出槽了。
- ⚠ **为什么不做「岸唇」**：曾把剖面做成「满深 → 回升越过水面 → 岸唇回落」，想让水带的几何边缘被岸埋住。实测只有约 **27%** 的水带边缘能达到水面之上 —— 地表网格在格边附近**只有角点顶点**，一格以外的地形全靠线性插值，**抬到水面之上的岸唇没有顶点去承载**，渲染出来依旧是水带边缘浮在地表上。因此回退成「几何只负责让水下陷，两岸交给颜色」（河床混色 + 湿岸带）。这条约束写在 `channelOffset` 的注释里，避免以后重试一遍。
- **岸色带全部按水带半宽缩放**：`wetScale / bankScale / floodScale` 是「水带半宽的额外倍数」，不是绝对半径。实测湿岸 1.58 / 河床 3.87 / 漫滩 7.74 单位（半宽 3.52），三层按「湿岸 ⊂ 河床 ⊂ 漫滩」层层收束。旧版写死绝对半径（6.6 / 10.1 / 17.6 单位）不随水宽缩放，窄段就成了「一条细线套一圈 11.5 倍半宽的巨大深色晕」、宽段几乎没有岸 —— 这是「河宽很丑陋」的另一半成因。植被与房屋的避让距离同理（`propsClearanceScale`）。
- **支流**：从内湖 / 内海湖岸出发（候选 39 处，实测生成 2 条），走同一套势能场，撞进已有水道即并入；**每生成一条就把它并进网络**，所以水系自然长成树枝状。支流沿源头到汇入口连续渐宽，并重进最近河段索引，因此河面、湿岸、避让、切槽和山体切口始终使用同一段插值半宽。v2.7 起支流**不要求入海**（`mouth` 为空、改报汇流点 `confluence`），因此不会出现「支流的河口指向旱地」。
- 渲染上复用 `render/ribbon.js` 的扁带，并把它**追加进统一水面的几何缓冲**（`render/water-surface.js` 持有那份缓冲与唯一一份材质，`render/river-layer.js` 只做「数据 → 顶点」的换算）。河流不再单独创建 `river-channel` 中泓材质；河床的真实下切由水深世界 Y 差转成 shallow → deep 色与 alpha。`river.renderSmoothing` 只做表现层的圆润化，不影响逻辑上的格边路径；水纹与海面共用同一张贴图与同一 UV 周期，滚动 `offset.x` 就是顺流向流动。v2.8 起河带末端还会**收尖**（见下文 v2.8）。
- **穿山河段共用中心线**：`nearestSegment(x, z)` 是河流的空间查询入口（返回 `{ d, w, mode, y }`，其中 `y` 是该处**局部河面**，v2.8 阶段二起不再是全图常数）。`world.heightAt()` 的河槽、`RiverLayer` 的河面与 `MountainField.surfaceAt()` 的山壳侵蚀都以该中心线采样，避免出现“水在一条线、峡谷在另一个地块中心”的双重几何。河流 revision 同时编码采样坐标、宽度和模式，任一项变化都会让山体场缓存失效。
- **山体网格必须消费这个切口**：数据层的侵蚀做好不等于看得见 —— 建网格时若用 `max(裸场, 地表)`，山壳会把切口重新盖回去。现在网格走 `MountainField.surfaceFrom`（与 `surfaceAt` **同一条混合公式**，只是接受本簇裸场以免每点做 25 次邻域查询：实测 `surfaceAt` 慢 7.7×、山体层会从 ~1.0s 涨到 ~2.8s）。存活单元判据也换成「**相对地表的厚度 > eps**」：侵蚀到贴地处厚度为 0，若仍发三角形就会与地表网格共面闪面，那块地交给地表与水带。实测修前 171 个河道横断面里 **169 个被岩石完全压住**、修后 **0 个**。
- **策划水路模式**：`mountainGorge` 与 `waterfall` 采用窄缝切口，`mountainPass` 采用宽豁口切口；`blocked` 会过滤该河段，`dryValley` 当前主要影响路径偏好。无论模式为何，河流仍只在势能下降且可通向入海或汇流终点的候选中选择，覆写不能制造逆流或无出口的河道。
- **侵蚀剖面的形状同样有语义**：`depth` 是「山壳退回地表的**比例**」，而混合是凸组合 —— 残余的 `(1 − depth) × 山壳` 只要高于水面，水带边缘就被压住；旧写法 `depth × (1 − t²)` 还从河心向外单调下降，即使 `depth = 1`，水带边缘也只剩 `1 − 1/widen²`。所以剖面改成「**水带宽度内恒为核心开通度、带外二次收束**」：`depth = 1` 就是整条水带完全开通；`< 1` 只能当「抬高河床的浅滩」实验。实测 `pass.depth` 0.72 → 86 个断面被压住、0.85 → 63 个、≥ 0.95 → 0 个，默认因此取 1.00（与 `gorge` 的区别只剩 `widen`）。

## 过渡带与格内次生特征

「不同地形之间怎么过渡」「单个地块里怎么不呆板」这两件事都放在 `render/props-layer.js` 里做，而不是另起一层 —— 它们与主植被共用同一套物种桶、同一套避让规则（道路 / 河流 / 山体）与同一套实例网格：

- **边界过渡装饰簇**：两种地形相接的边上按 `ecology.transitionRate` 撒一小簇「中间物种」（表在 `TRANSITION_SPECIES`：草|林撒灌木+阔叶+秋色，草|田撒灌木+作物，陆|岩撒碎石+灌木，陆|水撒岸边石…）。簇是往格边**两侧**甩的，压在同一条线上会像一条摆出来的队列。
- **格内次生特征**：每格按 `ecology.featureRate` 出现一处小景物 —— 水洼（只在平地上，`world.heightAt` 探一次坡度）、草丛、碎石堆（只在岩石类地块，否则满地灰石块像工地）、花簇/作物。同一地块因此是「有主景、有零碎」，而不是一块均匀的板。
- 地表的**格内微高差**（`height.innerRelief`）与中环细分是这一层的前提：中环顶点严格落在格内、不与邻居共享，才可以自由加起伏而不会破坏共享角点的一致性。
- 河滩混色：地表顶点色在河线附近朝 `palette.river.bedTint` 靠拢，并按河宽解析出的**三圈影响场**分层叠加 —— 漫滩（最宽，权重 0.42×）、河床（中等，0.78×）、湿岸（最窄，另叠一点 `water.foam` 提亮），最后整体略微压暗。`TerrainLayer` 里按世界位置计算，因此共享顶点算出来天然一致。只靠一条蓝色水带读不出「这是一条河」，水面必须配上湿岸。

## 五档道路

档位由「里数 + 端点城市梯度」推导（`config.road.grades`），表现由 `render/road-style.js` 统一给出配方，3D 与 HUD 的「道路一览」预览共用同一份配方与同一张贴图：

| 档位 | 里数 | 路面材质 | 构件 |
| --- | --- | --- | --- |
| 御道（铁轨） | ≥ 45 | 道砟 | 枕木 + 双钢轨（钢轨微凸带高光） |
| 官道 | ≥ 30 | 石板 | 两侧路缘石（抬起） |
| 商道 | ≥ 18 | 夯土 | 双条车辙暗线 |
| 乡道 | ≥ 8 | 碎石 | 沿路散石（实例化小石） |
| 小径 | < 8 | 泥土 | 毛边（路幅边缘抖出缺口） |

只靠宽度是不够的：默认视角下最宽与最窄只差两三个像素，等级根本看不出来，所以真正的等级信号是「材质 + 构件」。

跨地形时：跨水与跨河都抬平为**桥**（护栏 + 桥墩）；离岸 ≥ `road.trestleDeepWater`（默认 3 格）的**开阔**水面改用**栈桥**（窄桥面 + 高细密墩）；穿山脊的路段定为**隧道**，洞内路面不铺正常路面，而是压暗（`floorShade`）并略微下沉（`floorDrop`），两端各放一座**洞口**：卡在路上的六棱柱岩体（颜色由山脉配色派生，因此看起来是山的一部分）+ 两根门柱与门楣 + 深色洞口（两层暗面做出纵深）+ 门槛石阶。洞口半径跟着**路幅**走（`holePad`），否则最宽的御道会从洞口两侧溢出来。

跨河判定不能只看「采样点是否落在河道里」：道路采样步长约 7.04 单位，与收窄后的河宽同量级，完全可能出现两个采样点**跨在河两侧、而没有一个落在河里** —— 那样就判不出桥，路面会直接扎进 2.20 深的河槽。所以判据是「采样点落在河道内（带符号距离 < 0）**或**相邻两点的符号翻转」。实测 18 个陆上跨河桥采样点、异常 0；最低桥面 3.41，高于水面 0.11。

## v1.6：贴地视角下的「粘连细线」定位与修法

策划反馈「当前出现很多粘连的线」，现象是：贴地视角下有多条细长、淡灰、大致平行的半透明带横穿地图，压在树冠与屋顶之上。定位过程与结论：

1. **先排除坏几何**：遍历全场景，无 NaN、无异常长边；`bridge-piers 8.38` / `trestle-piers 13.46` 的实例缩放是「桥面高 − 地面高」（单位就是世界单位），桥跨水面 8 单位、栈桥跨峡谷 13 单位，合理 —— 不是被拉长。
2. **再逐层隔离**（同一贴地视角：仰角 16.7°、距离约 300，星陨城北）：
   - 只关「云影（贴地）」→ **长条全部消失**，画面饱和度恢复；
   - 只关「道路」→ 长条仍在。**主因是云影面片**。
3. **几何上的必然性**：地形最高 41.8，云影面片挂在 `y = maxRise + 0.6 = 42.4`，共 3 层（相差 0.35）；面片 2781×2326（比地图大 2.7 倍），一张 256 的贴图重复 7 次 → 1 个 texel ≈ 1.5×1.9 世界单位。贴地视角纵向压缩 `sin(16.7°) ≈ 0.29`，texel 行被压成间距 ≈2 世界单位的密排细线（用户截图上量到 13px @ 150px/格 = 1.9 单位，吻合）；`depthWrite:false` + `renderOrder 18` 且面片高于一切，所以它盖在树/屋/山之上。
4. **首修**：云影改为**贴合地形的网格**（`conformGeometry`：逐顶点 `heightAt + lift`，步长 `0.6 × hexSize`，覆盖 `1.12 × viewBox`）；3 层并为 1 层；贴图 512；开各向异性过滤（`sceneKit.maxAnisotropy()` 下发）；两个环境层**默认都关**。修后云影相对地形的偏移是常数 `1.100`（抽 253 点，min = max），即真正贴地。
5. **顺带发现的同类问题**：扁带只按中心线走高度，横坡上会浮边。修法见上文「扁带的横向贴地」，对照实测（同一场景两次构建道路层，只差 `conformDrop`）：

   | 左右两列离地偏差 | 修复前（三列共高） | 修复后（贴合地形） |
   | --- | --- | --- |
   | 中位数 | 0.85 单位 | **0.00 单位** |
   | 平均 | 1.77 单位 | 1.00 单位 |
   | > 1 单位的比例 | 26.4% | **11.0%**（＝桥 / 栈桥 / 洞内段的桥面，本该离地） |

6. 修完复测：贴地视角下开/关云影的画面**逐行纵向梯度曲线几乎重合**（中位数 4.92 vs 4.88，尖峰落在同样的行），说明细线不再由云影产生；剩下的横向条带是道路本身（御道 8.8 单位宽的路面在掠射角下本来就是一条带），属于设计内的观感。
7. **追加修复：山体峰顶扇面的索引错误**（策划随后反馈「山脉顶部和底部出问题，且各处山脉之间都会连接起来」）。`mountain-layer.js` 缝合三层环时，第三组的 `inner` 是**峰顶这一个顶点**，6 个扇区必须共用它，但代码写的是 `inner + k`：

   ```js
   const i0 = inner + k;            // ✗ k = 1..5 时读到的是「下一座山的基环」
   const i0 = toApex ? inner : inner + k;   // ✓
   ```

   每座山按 19 个顶点顺序排列（峰顶是第 19 个），所以 `apexIdx + 1..5` 正好落在**下一座山的基环**上 —— 每个峰因此朝四周射出又长又细的白刺，山脉之间被连成一片；最后一排山还会直接越出顶点缓冲区，拉出巨大的白片。问题正好出现在「顶部」（被错接的峰顶）与「底部」（被牵连进来的邻山基环），与反馈完全吻合。这个 bug 是 v1.5 写三段式山体时引入的，当时的断言只量了「顶点数与位置」，没量「索引拓扑」，所以漏了；现已补上两条：**三角形不许跨山**（三个顶点的 `floor(i / 每片顶点数)` 必须相同）、**索引不许越界**。（上面这组数字是 v1.6 的实测：0 / 1650。v1.7 把山体改成多环收口之后，每片从 19 顶点 / 30 三角形变为 **25 顶点 / 42 三角形**，这两条断言跟着升级，实测跨单元 0 / 4956、越界 0。）

## v1.7：统一平面微缩沙盘与河道重建

> ⚠ 这一节是 **v1.7 当时**的状态记录（保留以免后来者把当年的结论当新 bug 重新调查）。**v2.8 阶段二已把「统一平面」换成「逐地块档位 + 丘陵连绵波」**：`surfaceY` 不再恒为 0、丘陵也不再是「格内 dome」，详见上文「视觉风格」与「v2.8 第二阶段」。另：本节里「**地表**也读同一份环境解析结果」这句话自 **v2.9 起不成立** —— 统一地表的底色改取**配置地形调色板**（环境仍驱动水面 / 植被 / 天空 / 光照 / 底座），原因见下文「统一地表与地形混合（v2.9）」与方案 §15.28。

这一版把地形从「连续高度场」换成**统一平面**，并重做河流。改动最大的一处其实是**删掉一整套机制**：

- **所有地块共享同一个基准平面**：`tile.surfaceY` 与 `tile.cornerY` 恒为 0（实测 535 格全部为 0）。旧版那套「按通道给目标高度 → 拉普拉斯平滑 → 按 `reliefKeep` 回填山脉与峡谷 → 角点取三格平均 → 归一化到 `visualPeak`」的高度场整体移除，于是「相邻地块之间不能有台阶」这条约束**从算法上就不再需要维护** —— 大家本来就在同一个平面上。
- **地表只剩两种格内形态**：平原格心恒为 0（完全平，实测非平格心 0 个）；丘陵只在格内做一个 dome（半径 0.76 格、到格边回落到 0），实测 73 格丘陵、最高把格心抬起 3.83 单位，角点未归零数 0（相邻地块依旧无缝）。
- **峡谷与隘口体系整体移除，并且清干净了**：canyon 目标与实际都是 0.0%；逻辑 / 渲染测试里「连续高度场无缝性、隘口分布、跨峡谷栈桥、视觉峰值 41.8」这一批断言被换成「统一平面 / 格内微起伏 / 浅切槽 / 统一河宽」。上一版留下的一批**死配置与死分支**（`terrain.relief` 的 `pass*` 与 `canyonShare` / `minClusterSize`、`height.levels` / `reliefKeep` / `smoothPasses` / `selfWeight`、`palette.terrain.canyon`、`palette.outlineClass.canyon`、`ecology.cover.canyon`、`resources.canyon`、`props-layer` 的 `canyon|*` 过渡物种、`terrain-layer` 的 `CLASS_OF.canyon`、`mountain-layer` 的 `passDip*`）这一轮**已经全部删除**：没有任何代码会写 `tile.pass` / `tile.canyon`，留着只会变成静默失效的残留判据（同一个坑在 v1.3 改地形模型时踩过一次）。
- **山脉改为独立模型层**，不再由地表抬升表达；并重写了山体几何（局部取向主轴 + 共享格边高度 + 多峰剖面 + 残雪），见上文「山体（微缩沙盘）」。
- **河流重建**（顶点图沿格边寻路 + 水平水面 + 统一河宽 + 浅切槽 + 树状支流 + 跨河桥符号翻转判定），并修掉了根因 `Hex.edgeCorners` 的方向映射错误 —— 见上文「河流」。
- **环境系统（新增）**：`render/environment-state.js` 是唯一环境状态源（时段 / 季节 / 天气 / 自动轮播），`render/environment-palette.js` 负责「基础色板 × 当前环境 → 各层最终配色」。地表、植被、水面、天空、光照读的是同一份解析结果，所以切一次环境会整图一起变（实测主色 `#fff4dd` → `#bdb4b9`）。HUD 的环境卡片给出 4 档时段 / 4 档季节 / 4 档天气，外加一个自动轮播开关。

## 动态状态与生态

`src/world/tile-state.js` 把「动态状态」与「静态地形」分开：静态地图可由种子重建，动态状态可整体序列化（`serialize()` / `restore()`），将来直接交给服务端权威持有。

- 每格有 `moisture` / `fertility` / `growth`（确定性初值）
- 每条路有 `condition`，每栋房屋有 `condition`
- `tick(dt)` 做低频推演，`applyImpact({type:...})` 是玩家行为影响地图的接口（已实现且有断言覆盖）
- **磨损与生长本期不做演示**：`config.road.wearPerHour`、`config.props.conditionDecayPerHour`、`config.ecology.growthRatePerHour` 默认都是 0，`tick()` 会直接短路返回。把速率改成非 0 即可开始推演，渲染层无需改动

## 验证

逻辑层（世界生成、地形按比例、**逐地块档位 + 丘陵连绵波（v2.8 阶段二）**、**水下地表（岸线归零 / 跨格连续 / 离岸变深）**、浅切槽横剖面与河岸走廊、河流骨架与统一河宽、**河面沿程阶梯下降（单调不升 / 河口钉海面 / 落差真实存在）**、五档道路、桥 / 栈桥 / 隧道、**山体簇级噪声场（场值非负、轮廓外溢、逐簇归一化、窄脊带体量比、脊线峰谷交替、孤峰放射脊、低岩台、雪线）**、**演示大片山脉（20 格连成 1 簇）、山谷（A/B 对拍：谷心线压低 30%+ 且只作用在一侧）、脊带随簇宽增长与峰高随簇增长**、**山体 LOD（级别表 / 像素判据 / 单调切级 / 滞回）**、状态层、确定性重建）与渲染交互层（加载、WebGL、**三组可见表面拾取（统一地表 / 统一水面 / 水下地表）**、**统一地表的逐槽位底色（必须等于配置调色板且非纯白）与「陆地格心像素真的上色」（判据是饱和度，不是亮度）**、共享顶点颜色与法线一致、蜡笔笔触站点接续、**山体网格：逐 LOD 级按位置焊接 0 重复、表面/墙脚均不低于地表、防塌陷（退化 / 翻面 / 越界）、自由边贴地、默认视角省面比例**、**画面深度过渡：通道接线 + 俯视深度对拍 + 深度图采样步长与「屏幕锁定条纹」的行奇偶差（含 `nearest` 对照组）**、河面射线可见性、植被与房屋的避让、过渡带与次生特征、扁带横向贴地与桥面不贴、云影网格贴地、点击地块 / 城市、拖拽缩放、图层开关含山体与河流、相机切换、HUD 道路一览与三轴环境控件、云鸟与云影动画）都做了断言验证。

当前逻辑测试（**249 项**）与渲染/交互测试（**190 项**）均通过（合计 439 项），运行期无 JS 错误。逻辑测试覆盖 `WorldRebuild`、策划覆写到真实河段模式与山体侵蚀、河流 revision 驱动的山体缓存失效以及确定性重建；渲染测试覆盖页面启动、WebGL、山体 LOD、河流与水深预通道、河源水体、Picker、图层开关与环境交互。应用级重建已纳入渲染自动回归：断言 world 与地形 / 山体 / 河流图层组被替换、旧图层组 `__hexLabDisposed`、场景根节点数不增长、Picker 与旅行模拟重建、仅改山体风格时河流 revision 不变、`mountainPass` 覆写后河段模式与河心侵蚀（1.000）生效，以及 `dispose()` 后图层组全部移除。山脉重掷也进了回归：点「重掷山脉」后种子改变、山格位置改变而总数只在演示块范围内浮动、场景根节点与 Picker 完好；点「还原默认」后山格逐格还原。悬浮条的位置也在回归里：它必须落在左上角、且挂在 HUD 面板**之外**的独立浮动层上（面板的 `backdrop-filter` 会给 `fixed` 后代换掉包含块）。

**穿山可见性**（v2.4）是这一轮唯一的修复目标，断言分布在两侧：逻辑侧断言**水带的每个横断面都必须开通**（沿水带横向取 5 点求最低山壳，落在水面以下；实测 181 个断面被压住 0 个、最高残余 −0.080 单位）、**侵蚀剖面在水带内是平的、只在带外收束**（旧剖面在水带边缘只剩 0.65，这条会直接为 0；实测水带边缘与河心同深 171/171），以及**未被侵蚀处表面与旧的 `max(场, 地表)` 逐点零漂移**（714 点零侵蚀 / 197 点在侵蚀区、漂移 0 个，保证修复不顺手改掉整张地图的山形）；渲染侧直接验**网格产物**：最细一级网格落在水带内的表面顶点不许有一个高于水面（33 个、高于 0 个），并把山体网格加进「河面是最上层可见面」那条射线断言的目标集合（此前只放了地表 + 水面，等于把唯一可能盖住水的山壳排除在证据之外 —— 修前正是它把整条河埋住）。

⚠ 一条容易重犯的坑：山体几何的判据必须是**含侵蚀**的共享公式（`field.surfaceFrom`），且按**本簇**的场判（块之间靠包络外溢重叠，跨簇处全局场取的是邻簇的更大值，实测差可达 8.5 单位）。原判据写的是 `max(field.fieldAt, heightAt)` —— 拿网格自己用的公式验网格，是自证，所以山壳埋住整条河时它照样绿（详见方案 §15.21）。

**水体显示三项**（v2.5）的断言与量化：① **水陆基色隔离**在逻辑侧逐点对拍（804 个水陆交界角、最大偏差 0，另带「把水格混进去颜色必差」的对照组）；② **水色不随视角变**在渲染侧实拍同一水面像素 × 6 组位姿（正交 / 透视 × 俯角 × 视距），关雾后极差 **Δrgb(0.4, 0.6, 0.7)**，并另设一条**开雾对照**断言（极差 ≥ 10，实测 48/37/35）证明探针不是空转 —— 雾按距离改色是刻意保留的表现，本轮不动它；③ **河源水体**：竖直射线 45 条验「水面是最上层可见面」（被挡 0 条）、「水面水平且半径 = 碗半径 × water 比例 × 岸线扰动（实测 0.900~1.096）」、「最小方位半径仍盖住河源顶点」，逻辑侧还验碗的支撑正好是那个圆（1.02 × 半径外与「无泉」世界零漂移）、除河源顶点外所有共享角点零漂移、以及**路压在水面上时必须抬成桥面**（实测 2 个采样点、沉入 0）。

⚠ 三条踩过的坑（详见方案 §15.22）：**判据要打到产物为止**（解析式 `heightAt` 说碗没问题，但地表网格每格只有 13 个顶点，碗太小就解析不出来 —— 只有竖直射线打到网格上才看得见 33/45 条被顶穿）；**`gl.readPixels` 的 y 从下往上**（上一版探针漏了翻转，读到纵向镜像的像素，量出来的「水面极差 132」是假的）；**一个几何装多片圆盘时索引要带每片的起始顶点号**（漏掉只有第 1 片打得中）。

**水体显示（v2.6）**是「水面上奇怪的色块」的一次收尾，三件事都是**「数据的粒度 / 边界假设」错了**，而每一件都只有把画面当判据才看得见（详见方案 §15.23）：

- **水深只有 3 个档位**：`depthRatio = (distToLand − 1) / depthRamp` 里的 `distToLand` 是**整数格距** ⇒ 全图只有 0.352 / 1.003 / 1.654 三档，相邻水格最多差 40% 的过渡量；画面深度过渡把这三档原样放大成一块块**硬边多边形**。现在改成「到最近陆地格六边形的**连续**距离」场：格心处与旧值逐值相同，点与点之间连续（暴力对拍 1292 点误差 2.2e-16），同一档位内水深极差 0.297（旧版 **0**），相邻水格 Δt>0.2 的比例从 40% → 16%。
- **地图外被当成陆地**：海底在沙盘边缘抬回水面（外缘 182 个格边采样点高度**全为 0**）⇒ 外缘一圈水莫名变浅发亮、水与海底共面。现在地图外按**开阔水域**算，182 个采样点全部 < 0（最高 −0.40）。
- **海底被加了格内微起伏**：`innerRelief × maxRise ≈ 1.25` 单位 > 水深 0.35~1.48 ⇒ **272 / 948 个中环顶点被抬到水面之上**（最高 +0.997），海底在整片水面上穿出一圈圈硬边格子斑块 —— 这才是截图里那些「板块」的**主体**（把水下地表隐藏起来，色块立刻消失）。现在这层微起伏**只给陆地**。
- 顺带**纯水只用基色**（水面顶点色实测 2054 个顶点除河口湿岸 8 个外**全等**；旧版顶点色的亮度极差 30.4%、相邻水格 25%，全部来自那条大尺度手绘斑驳 `patch`）—— 深浅只由深度过渡给出，符合「纯水应该是画面深度呈现的效果」。

这条链上最值得记住的是：**「深度过渡」读的是渲染网格里的海底形状，不是 `heightAt` 的解析值** —— 公式对、断言绿，仍然可以在画面上穿出一圈圈斑块。所以新增的断言都直接读**网格顶点**：水下地表 2054 个顶点最高 y = 0（不得高于水面）、水面顶点色是常数、以及逻辑侧那份「深度 = 连续离岸距离函数」的暴力对拍。

**山体与水的交界（v2.6 第二段）**收的是用户截图里另外三处，其中两处同源（详见方案 §15.24）：

- **山体在邻格上铺了一圈「贴地薄壳」**：网格器按「簇包围盒 + 外扩」铺规则网格，而场在包围盒边缘很平 ⇒ 厚度只是趋近 0 的浮点噪声；旧存活判据「厚度 > `GROUND_EPS = 0.02`」把它们全部留下，于是每座山外面有一圈**贴地薄壳**按 `max(场, 地表)` 盖住邻格的草地 / 农田、却用着岩壁贴图 —— 远看就是「**每座山外面一块更暗的方形面片，山格正在正中间**」（方形来自网格器自身的规则边界）。实测 **269 / 454 个非山格心被盖住**、最厚 4.88 单位、72.8% 薄于 0.5 单位。现在按一条原则收掉：**看不见的山体就当它不存在** —— 地表低于水面、或相对地表厚度 < `footMin`（0.03 格 ≈ 0.66 单位）的采样点，表面一律取地表 ⇒ 实测 **0 / 454**；山体三角面 113402 → **72694**（v2.8 阶段二地形高度改动后为 **70576**）、落地墙 5174 → **0**（表面在轮廓线上就等于地表，墙顶与墙脚焊成同一个索引、三角形自动退化），而「山脚越过簇边界 282 / 282」与「外溢 ≤ 0.42 格」**保持不变**（没有为了让斑块消失把山脚收回去）。
- **`max(场, 地表)` 把山体抬到水面之上**：水格的「地表」是**负的水下河床**，取 max 之后临海的簇在海面上铺出一块块**方格石板**（实测水格上 181 / 3950 个采样点高于水面、最高 8.46 单位）。修法与上一条同一行判据，只是多一个条件（`g < 水位`）—— 只钳到水面**之下**也不行，浅水里留的一层岩石皮在水面半透明下照样看得见。实测 **0 / 158**。水位不在这里重抄 `size × water.level`，统一问 `Rivers.waterLevel(size)`。
- **一个被实测证伪的提案**：曾经按 Fresnel 直觉加过「水面掠射趋不透明」项（`t = max(t, gate)`，区间 `[0.08, 0.35]`）。写完后曲线单调、端点正确、6 条断言全绿，但**同姿态、固定像素集的 A/B** 显示它对画面的真实影响只有 **0.04%**（默认视角）~ **7%**（最平视角）：判据的生效区间（`|视线.y| < 0.35`）整个落在深度过渡**已经饱和**的区间（`viewCos < 0.42`）里，`max` 抬不动 `t`。已全部删除，两处注释写明理由 —— **新增视觉项必须先做同姿态 A/B，「代码执行了」不等于「画面变了」**。
- **一条「屏幕锁定」的水平分界线**（用户最初记为症状③；「**分界线在场景中间、水平、平移场景不动**」这句话直接把它钉在**屏幕空间**）：先逐个排除世界空间的候选（阴影 / 河床贴图 / 镜面高光 —— 改 `roughness 1 / metalness 0` 只让均值 145.2 → 146.9；把**水下地表网格**整个藏掉，条纹**仍在**，所以它不是上面那三个海底问题）。真正定位它的是**扫分辨率参数**：`depthResolution` 取 0.5 / 0.75 / 1.0 ⇒ 行奇偶差 **35.94 / 10.51 / 0.42**，条纹强度与深度图的 texel 格严格同步。根因是 WebGL 的**深度纹理只支持 NEAREST 采样** + 预通道半分辨率 ⇒ `t` 按 2×2 屏幕像素分块。修法是水面片元里**手动双线性**（4 个 texel 各自还原到视空间再加权、背景权重置零后归一化、无效回退最近邻），实测 **35.94 → 0.21**，切回 `'nearest'` 的对照组 **18.05**。教训：**「离散中间量被下游连续算子放大成台阶」这次放大在屏幕分辨率上** —— 用低分辨率纹理（尤其深度纹理）驱动逐像素量之前，先问它能不能插值。

⚠ 顺带修掉一个隐性坑：`water-depth.js` 的 `applyUniforms()` 原本只在 `create()` 时读过一次配置，**运行期改 `Config.water` 完全无效**（与「策划改完立刻可见」的目标冲突）。现在 `update()` 每帧重读。这也是上面那次 A/B 一度得出「门开＝门关」错误结论的根因 —— **对照组如果「什么也没改变」，先怀疑对照自己有没有生效**。

**水体与河流（v2.7）**是用户带 Civ6 参考图报的四件事（**偏白 / 没泡沫 / 河口没连接 / 看不到波浪**）加一条生成规范（所有河流必须有源头和入海口，入海口接入大片纯水）。表现侧三个根因都是**旧账**，也都能用数字钉住（详见方案 §15.25）：

- **颜色链有两份**：`water-depth.js` 在颜色之外另存了 `uDepthShallow / uDepthDeep`，同一份 `palette.water` 被读两遍、混两遍 ⇒ 水色被往「淡」里拉了两次（实测水面均值 **RGB(203.7, 229.2, 235.3)**、饱和度 0.140）。现在 `water-depth` 只提供深度与共享 GLSL，**不再拥有颜色**。
- **注入点把贴图与顶点色整体覆盖**：旧写法注入在 `#include <color_fragment>`，而 r147 的 `<map_fragment>` 与 `<color_fragment>` 都在它之前 ⇒ 把 `map` 置空、`vertexColors` 关掉，画面差 **Δ0.000**（水纹贴图与逐格顶点色一直在空转，河流的顺流 UV 也看不出在动）。现在换成自写 `ShaderMaterial`（`render/water-material.js`），贴图与顶点色**真正参与**颜色，光照改成手绘 **cel 色带 + 阶梯高光**。
- **波浪看不见有两条原因**：振幅是唯一**漏乘 hexSize** 的水体参数（0.045 当世界单位用 ≈ 格距的 0.2%，实测把振幅 ×20 才让 0.97% → **59.37%** 的水像素变化），而且波浪做在**顶点**上（水面每格只有 13 个顶点、间距 ≈ 11 单位 < 主波长 16 单位）。现在波浪**全部做在片元**（世界 XZ 的 sum-of-sines + 解析偏导算解析法线），实测振幅 **1.540 = 0.07 × 22**、波长 57.2，与网格密度和相机分辨率都无关，也不需要顶点位移（水面与陆地共面）。
- **泡沫**改成按**真实水深**生成（旧判据是「波峰高度」，与水深无关 —— 深水里当然不会出现岸边泡沫），海 / 河 / 湖 / 河岸同一套；`ink-layer` 那条静态泡沫通道删除。**河口**改用顶点属性 `aMouth`（0 源头 → 1 河口）把河色混向**当前海色**（混合目标与海面共用同一份 uniform，环境切换后河口跟着变），并生成 2~3 条**分流带**与一圈**冲积带**色（只做表现：实测不改地块分类 / 高度 / 寻路）。旧的 `mouthOffset` 只改末端一个采样点 **0.012 单位（格距的 0.05%）**、不可见，已删除，换成渲染层 `renderBias`（0.264 单位，逻辑水位不变）+ `mouthBlend`。
- **河流规范**：把「必定入海」从概率改成**结构** —— 水格先做**连通域**，势能场 BFS 只以「面积 ≥ `seaMinBodyTiles`（12）的连续纯水」为种子。必要性是量出来的：默认地图的 10 个连通域是 **140 / 5 / 4 / 2 / 2 / 1×5** 格，而旧世界里只有 **2/6** 条河真的走到大片纯水，另 2 条的河口指向**旱地**。改完 4 条主河 `reachesSea` 全为 true；**支流不要求入海**，改报汇流点 `confluence`；走不到水又未汇流的轨迹按 `onDeadEnd = 'drop'` 丢弃（默认世界丢 1 条）。⚠ 代价写在明处：主河从 5 条降到 **4 条** —— 少掉的那条本来就没入海。
- ⚠ **一条探针自己出错的教训**：换材质之后「关雾跨 6 组位姿极差 ≤ 10」失败（13.4 / 16.4 / 17.4）。我先给探针加了一层「抹平逐像素细节」的隔离档，结果 raw 与 flat 的数字**完全相同**、守卫报 **0 份材质** —— 隔离开关根本没传进 `page.evaluate`，一次都没执行；真正让极差掉到 **3.8 / 3.6 / 3.2** 的是顺手补上的 `layers.ambience.setTime(0)`（云雾 / 云影按真实墙钟滚，**漏冻一个图层就够漂 4 倍**）。那套隔离机制随后整段删除：**探针的隔离与对照必须自证生效**，否则结论会建在「对照什么也没改」上。

**统一水面（v2.8 第一阶段）**收的是用户下一轮报的两个症状与一条需求变更：**「河流和纯水地块还是没有衔接」**、**「还是能看到河流的方头结尾」**，以及**「水面可以不是绝对平的，允许有波动」**。诊断脚本（`diag-mouth.js`）把「方头 / 不衔接」拆成四个根因，全部是**结构问题、调参解决不了**：

- **R1 —— 同一个水位被拆成三个高度**：海 / 泉 / 河各有一套 `renderBias`（0 / 0.006 / 0.012 × 格距 = 0 / 0.132 / **0.264** 单位），实测三张水面分别落在 y = 0 / 0.132 / 0.264。河口因此有一级 0.264 的台阶，河面读起来是「一块浮在海面上的板」。`renderBias` 已从配置与代码里**彻底删除**（断言直接检查 `renderBias === undefined`）。
- **R2 —— 河面末端停在海岸线上、截面是平的**：`mouthOffset` 只挪末端**一个采样点** 0.012 单位（格距的 0.05%），等于没挪。现在新增**河口延伸段** `river.mouthRun`：把中心线插进**水格内部**，方向用 ±72° 探测取「离陆地最远」的那一支（河线与海岸线近乎平行时直线延伸只会走 0.13 格，探测后实测 37 个延伸采样点、末端离最近陆地 0.87~1.04 格）；河带末端再按 `water.mouthTaper` **收尖**（末 3 个采样点半宽线性收到 0.35×，实测收尖 57 个采样点）。「平头截面」因此变成逐渐收口的尖头。
- **R3 —— 河口分流没有裁到水格内**：分流按逻辑中心线直出，会插进山体 / 森林。现在逐点裁剪到水格内（保留分叉点那一段以免与干流断开），因此实测 12 条分流**全部落在水格上**、起点仍落在干流中心线上。
- **R4 —— 支流与干流共面**：三份水面各自参与透明排序、互相重叠，共面处只剩 z-fighting。**这正是「调参解决不了」的那一类：问题在结构上。**

所以 v2.8 直接把水面**合并成一份几何 + 一份材质**（`render/water-surface.js`）：海（按 `water.wave.subdiv = 2` 加密到每格 19 个顶点，为顶点位移留采样密度）+ 河（含分流）+ 泉 / 湖圆盘全部写进**同一个 BufferGeometry**，水型差异**只走逐顶点属性** `aMouth`（0 源头 → 1 河口）/ `aDelta`（分流带标记，供 HUD 开关）/ `aPalette`（0 海 / 1 河 / 2 泉）/ `aProfA`（波幅 / 波速 / 吸收 / 泡沫带宽）/ `aProfB`（透明度下限 / 深度淡出 / 流向 / 水纹强度）。实测**全场景只有 1 个水面网格、1 份材质**（阶段一时 **12928 个顶点** = 海 3002 / 河 9282 / 泉湖 644；阶段二河面按地块档位重排后为 **12934** = 海 3002 / 河 9288 / 泉湖 644），12 条分流带 **312 个顶点**带 `aDelta`，`aMouth` 覆盖 0 → 1。接缝、重叠、z-fighting 与「三个高度」**在结构上不再存在**，整片水一次绘制。

「允许波动」因此落在两个地方：**几何保持单一水位**（阶段一的硬不变量；阶段二**有意放宽为「只有海面是平面」** —— 河 / 泉各取自己所经地块的档位高度，见下一条），**波动交给 GPU** —— 顶点着色器用与片元**同一段** GLSL（`waterWaveAt`，sum-of-sines + 解析偏导）做位移，所以剪影与明暗一致；位移只给开阔海面（`waveAmp`：海 1 / 河 0 / 泉 0），幅度按 `hexSize` 换算、实测 **1.540 单位 = 0.070 格**（上限 0.08 —— 「剪影看得见起伏，又不掀翻浅滩」）。

代价与连带清理写在明处：河 / 泉**不能再单独隐藏**（几何已合并），HUD 图层开关合并为「水面（海 / 河 / 湖）」一个总开关，原「河流」那一格改成「河口三角洲」（几何合并后它只剩 `uDeltaOn` 一个 uniform 开关，仍可开关且已加断言）；`ink-layer` 新增 `coveredByRiver`，被河面压住的格边**不描边**（否则河口会横一条墨线，实测跳过 37 条）；`water-material` 的 `setPalette` 把三套色板一次刷进来，**河口混色目标就是其中的海色**（环境改海色时河口自动跟随，不需要第二份「河口目标色」）。⚠ 一条容易踩的坑：`TerrainLayer.createBuf()` 与 `Ribbon.createBuf()` 的顶点缓冲**字段名必须一致**（`pos / col / uv / idx`）—— 统一水面是「地表缓冲 + Ribbon 扁带」混着追加的，两边字段名不同会在启动时直接抛 `Cannot read properties of undefined`。已统一为 Ribbon 那一套并加注释锁住。

**逐地块高度 + 河流沿程下降（v2.8 第二阶段）**收的是用户接下来的需求：**高度场 / 地形混合 / 逐地块连续高度 / 河流沿程下降**。用户定的顺序是「先地形高度，后贴图混合」，本轮只做高度；几处关键取舍也由用户拍定：**丘陵 = 「一点点圆润连绵的波」**（明确否掉「单档平台 + 一格斜坡」）、**河流按地块类型阶梯下降**、**山格不抬基座**、丘陵量级 **≈ 0.10 格**、**河源档 = 丘陵档**（侵蚀目标改为局部河面）。

- **高度收口到一个模块**（新文件 `world/height-field.js`）：档位表（`tiers` / `tierOf` / `baseTier`）/ 地貌噪声与丘陵阈值 / 丘陵掩码与连绵波全在这里。它同时被五处消费（hex-world、river-builder、spring-layer、mountain-field、terrain-layer），收口的理由是 v2.7 / v2.8 两次翻车都是「同一个量在多处各解释一遍」（三份渲染偏置、两套水位）。
- **丘陵：从「一格一个 dome」换成「连续连绵波」**：旧版丘陵是**逐格**的（圆心在格心、还带随机偏移、半径 0.76 格、格边必须归零），于是每个丘陵都是孤立小圆包、相邻丘陵之间必然有一条凹缝。现在是一个**世界坐标上的低频场**（均值 = 丘陵档、峰值 = 2 × 丘陵档），跨格天然连续、共享角点自动一致，**「格边归零」这条约束整体删除**。实测：丘陵 81 格、格心均值 **2.189 ≈ 丘陵档 2.2**、峰 3.69 单位（用户要的「整体观感还是平的，只是有点高度感」）。
- **丘陵波的两道门控必须是连续的**：① 山 / 城（山格不抬基座、城市格恒平）用 `hillSetDistance` 的**连续距离场**平滑收掉；② 岸线用既有的 `edgeFade(waterEdges)`。⚠ 写成布尔判断会出事 —— 同一个物理角点从一个格查是 0、从另一个格查是波的峰，网格上就是一道**3.75 单位的裂缝**（实测 10 个角点）。改成连续场之后 **0 处裂缝**。同理，**山格 / 城市格的「档位」也拆成两个函数**：`tierOf`（水文档，山格取丘陵档，供河面与侵蚀）与 `baseTier`（地表基座，山格恒为 0）—— 混用一个会让每座山的壳整体抬高一个档（实测雪线断言立刻抓到 41/81 格全在雪线之上）。
- **城市格的基座必须在改成 `city` 之后重算**：城市格是在地表基座写完之后才被改成 `city` 的，不重算会留着「改之前」的档位（实测 5 座城建在丘陵上、档位停在 2.2，HUD 与山体基座都会读到错值）。
- **河面沿程下降**：见上文「河流」一节（单调不升、河口钉海面、落差 1.406、7 级台阶）。⚠ 两个顺序坑都留了注释与断言：① 段索引里存着逐段的河面高度（地表切槽的 ceiling、山壳侵蚀、渲染河面都读它），**必须在高度定稿之后重建**，否则「地表按旧水位切、水面按新水位画」—— 现象是河被地面埋掉（实测 322/748 个水带边缘采样点的地面高出水面 1.4 单位）；② 河口分流要在**定稿之后**再建，否则 `fork.y` 是旧值（实测 river-0 干流全程 0、分流却从 2.2 起，三条分流凭空悬在河口上方一个档）。
- **连带清理**：`hillAmp` / `hillOffsetX` / `hillOffsetZ` / `microNoise` / `cornerY` 与 `height.floor` / `height.discreteBands` 全部删除；`relVal`（「同一个起伏通道两个消费者」的另一半）随丘陵 dome 一起删除；`landformRatios` 的 `lowland` 统一为 `plain`；岩壁贴图按 seed 记忆化（地表层与山体层原本各生成一张一样的）；`mottleTexture` 拆出 `mottleCanvas`（草地底纹不再先造一个没人持有的 Texture 再取它的 canvas）；`mountain-layer` 导出的 `heightAt` 改名 `surfaceHeightAt`（与 `world.heightAt` 同名两义正是「水面之上到底有没有地形」各说各话的源头）；`HeightField.CEILING_EPS` 把「地表必须切到水面之下多少」抽成一个常量（旧版实现写 0.02、各条断言各写 0.015 / 0.02 / 1e-9 / 1e-4 / 1e-6，同一个物理约束五个阈值）。

**统一地表与地形混合（v2.9）**收的是上一轮按用户拍定的顺序留出的「贴图混合」，实施中用户又报「河流和纯水地块交汇处仍能看出条带面片，让所有水体融为一体」，于是同轮两件事一起做：**贴图级 splat**（新增 `render/ground-material.js`：五张灰度贴图按逐顶点权重混合 × 逐槽位底色，地表从「按地形分组的五张网格」合并成**一份网格 + 一份材质**）与**水体融合**（岸线 0.9 格湿沙混合带由新增的 `world.waterDistance` 连续距离场驱动；`Ribbon.pushBand` 扩成左右各自取宽，水格上的河带裁掉那半边；河口因子 `aMouth` 从「按弧长比例」改成「按离水距离」，并新增 `uSeaProfA / uSeaProfB`，让河口处的**剖面参数**（不只颜色）也朝海面混合）。地表之所以留在标准材质上（`MeshStandardMaterial` + `onBeforeCompile` 的最小注入，只替换 `<map_fragment>` 一处），是为了**免费继承**雾 / 阴影贴图 / 光照 / tone mapping —— 换自写 `ShaderMaterial` 就得把这几样逐个手工接回去（水面是特例，它不吃 PBR）。

这一轮最值得记的是**「整图变白」的两个根因，都不是美术问题**（用户截图：除了山脉和水，别的地块全是白的）：① **注入落空** —— 底色 uniform 原本在 `onBeforeCompile` 里现造，而 `applyEnvironment()` 在**首次渲染之前**就执行，此时 shader 还没编译 ⇒ 那批 uniform 还不存在 ⇒ 注入静默早退，编译时 uniform 用纯白初始化；而顶点色此时只剩「明暗比值」（≈1.0），灰度贴图乘纯白就是**全白**。修法是把底色提升为 `create()` 时就建好的**持久对象**并按引用交给 shader，**结构上消除「编译时机 vs 注入时机」的耦合**。② **颜色空间错** —— uniform 里的色号没有线性化，而明暗比值是**线性空间**的量（`linear(顶点色) / linear(类别底色)`）⇒ 分子分母差一次 sRGB 传递函数，底色偏亮近一倍、r/g 饱和到 255（同样读成「发白」）。修法是统一 `convertSRGBToLinear()`。两条合成一句：**「先注入、后编译」的数据必须有独立于编译时机的持有点；比值型颜色方案里，分子与分母必须在同一个空间里量。**

还有一处**不是 bug 而是设计取舍**：底色接到环境色板（`env.terrain.*`）后地表明显偏淡（实测草地 `rgb(240,254,182)`，改造前是中饱和绿）—— 根因是 `role()` 把季节 tint 当「**向 tint 混色**」，而配置里夏季预设的 tint 是 `0xffffff`，等于把草地朝白混 **28%**；这份「向白混过」的值本来只服务道路预览底色等消费者。用户拍定：**底色取配置地形调色板**（与明暗比值的分子同源 ⇒ 纯色地块与改造前逐位一致，splat 只负责交界渐变）。代价写在明处：**季节 / 时段不再改地表底色**（与 v2.8 之前的行为一致），环境仍驱动水面、植被、天空、光照与底座；这条口子已写进 `environment-palette` 的注释，防止下次再被接回去。

新增的两条断言都是「打到产物为止」：材质里的逐槽位底色必须等于**配置调色板（线性化后）**且非纯白（与调色板偏差 0，实测 `grass#96b35a = 0.305/0.451/0.102` …）、**陆地格心像素必须真的上色** —— 判据用**饱和度**（`max − min`）而不是亮度：这套手绘风里朝上的地表受光本就足、亮度偏高，拿「亮度低」当判据会把正常画面判成失败，而「全白」的特征恰恰是三通道近乎相等（修前 `rgb(255,255,245)` ⇒ 饱和度 **10**，修后代表格 `rgb(199,217,118)` ⇒ **99**）。⚠ 探针必须**关雾 + 冻结动画**（雾按距离把地表朝近白拉），且 `readPixels` 之前必须**立刻**渲染 —— `preserveDrawingBuffer` 为 false，任何一次合成（含截图）都会把绘制缓冲清掉、读到全 0（与 §15.22 同一条坑）。

山脉相关的断言跑在 **`WorldRebuild` 路径**（= 页面实际路径）上：早期它们跑在裸的 `HL.World.build()` 上，而 `MountainSystem` 只在 `WorldRebuild` 里装配，于是「地图有没有山谷」这类判定验的是一条应用从不使用的分支 —— 那条分支带谷、应用那条一个谷都没有，测试却是绿的。另有三条防静默失效的断言：**带谷簇数必须既不是 0、也不等于簇总数**（概率门控最典型的坏法不是报错，而是分支永远走不到）、**规则型覆写只命中匹配的地形分类且计入 revision**，以及**换山脉种子后非山格逐格零漂移**（「只重掷山脉」的判据本身）。

本目录自带这两个脚本，可直接复跑：

```powershell
node tests/logic-test.js     # 纯逻辑，不需要浏览器
node tests/render-test.js    # 无头 Chrome 实跑：渲染 / 拾取 / 交互
```

`render-test.js` 依赖 `puppeteer-core` 与系统 Chrome；它会先尝试本目录与上级原项目的 `scripts/e2e/node_modules`，也可以用 `PUPPETEER_PATH` / `CHROME_PATH` 指定。

⚠ **渲染测试不产出任何文件**：`render-test.js` 只打印断言结果，不写图片、不建目录。早期版本会把截图落到根目录（`preview.png`）与 `tests/out/`，这段逻辑已经移除 —— 跑多少次都不会在工作区里多出东西。

关键实测数据（v2，全部由上面两个脚本实测输出）：

| 项目 | 数值 |
| --- | --- |
| 地块 | 535 格（含 88 格边界海岸） |
| 地形占比（剔边界 447 格，目标 → 实际） | 水 18.0% → 15.7%（70 格），草地 29.8% → 27.1%（121），农田 14.9% → 13.6%（61），森林 19.0% → 19.0%（85），花田 4.1% → 3.6%（16），**山脉 14.2% → 18.1%（81，含演示块 20 格）**，峡谷 0% → 0% |
| 高度（v2.8 阶段二） | **地表基座**只由地块类型决定（水 / 平原 / 城市 / 山格 = 0，丘陵 = 0.10 格 = 2.2 单位）；**丘陵连绵波** 81 格、格心均值 **2.189 ≈ 丘陵档 2.2**、峰 3.69 单位（旧版是逐格 dome + 格边归零，已删）；平原（丘陵带 / 走廊之外）格心非平数 **0**；**共享角点裂缝 0 处**（同 XZ 的重复顶点 y 逐位相等；布尔门控时实测 10 个角点、最大 3.75 单位） |
| 起伏形态 | 最大山脉簇 **22 格**（演示块，前五 22 / 12 / 11 / 6 / 6），最小簇 1 格；生成部分平均簇宽 3.1 格、最长 14.7 格、占陆地 17.1% |
| 离城距离 | 山脉最近 2 格（下限 2） |
| 山体（最细一级 = detail 12，v2.6） | 81 格 / 17 簇 / 孤峰 5：**37565 顶点 / 70576 三角形**（**全部是表面，落地墙 0**；修复前 59949 / 113402，含落地墙 5174），最高 32.4 单位；每簇实测峰高 === `amp`；**重复位置 0、表面超差 0、墙脚超差 0、翻面 0、越界 0、悬空自由边 0、退化面 0**；**非山格上不得有山体：454 个格心被盖住 0 个**（修复前 269 / 454、最厚 4.88 单位）；**水面之上不得有山体：158 个水下格心被盖住 0 个**（修复前水格上 181 / 3950 个采样点高于水面、最高 8.46 单位）；山脚越过簇边界 282 / 282 条；外溢 0.42 格（上限 0.52）；雪顶 41 / 81 格、近雪色顶点 4459 个 |
| 山体 LOD（v2.6） | 四级 `[12, 8, 5, 3]` = **70576 / 32378 / 13402 / 5216** 三角（各 17 块；修复前 113402 / 53172 / 22661 / 9490）；逐级：重复位置 0、表面超差 0、墙脚超差 0、翻面 0、越界 0、悬空自由边 0、退化面 0；默认视角（1250）实际渲染 **13402 / 70576 = 19.0%** |
| 穿山可见性 | 河道横断面 **200 个：修前 169 / 171 个被岩石完全压住（v2.4 记录）→ 修后 0 个**；最细一级网格落在水带内的表面顶点 **33 个、高于水面 0 个**（v2.4 时为 597 个；v2.6 薄壳消失后水带内的表面顶点随之减少，判据仍是「高于水面 0」）；垂直射线 190 处**被山壳挡住 0 处**（河面命中率 98.4%）；穿透处侵蚀 1.000；未侵蚀处表面与旧的 `max(场, 地表)` 逐点零漂移 |
| 岸边水面透出的东西（v2.6 新增） | 在「水面盖住的像素」上把水**背后**的一切从**主通道**关掉（`layers.disable(0)`，深度预通道所在 layer 1 原样保留 ⇒ 过渡系数 t 不变），量 Δ背后：俯视 0.30 **64.94**（>8 占 90.8%）、默认整图 0.86 **33.06**（68.0%）、更平 1.15 **6.58**（13.6%）、很平 1.45 **5.99**（7.8%）；判据 = **逐档不增 + 首尾 ≥ 5×**（实测严格单调；1.15 档已贴地板 ⇒ 不要求每档都留余量）；俯视那条同时当对照，证明探针真的能看穿水面（不是空转） |
| 水面「屏幕锁定条纹」（v2.6 新增） | 症状：屏幕上一条**水平**的「分界线」，固定在屏幕位置、**平移场景不动**；逐行亮度实测奇数行 ≈ **118** / 偶数行 ≈ **170**（**每 2 行跳一次**）。扫 `depthResolution` 0.5 / 0.75 / 1.0 ⇒ 行奇偶差 **35.94 / 10.51 / 0.42**（与深度图 texel 格严格同步）⇒ 根因是**深度纹理在 WebGL 里只支持 NEAREST 采样** + 预通道半分辨率 ⇒ 过渡系数 t 按 2×2 屏幕像素分块。修法 = 水面片元里**手动双线性**（4 个 texel 各自还原到视空间再加权、背景 texel 权重置零后归一化、无效回退最近邻）；实测行奇偶差 **35.94 → 0.21 / 0.17**，切回 `nearest` 的对照组 **18.05**；采样步长 = 1/**深度图**尺寸（640×400 ⇒ 1.56e-3，**不是** 1/主画面 1280） |
| 构建耗时（软件渲染） | 分层：地形 380 / **山体 3457** / **水面 79** / 描边 40 / 道路 54 / 植被 45 / 村落 17 / 城市 15（单次采样；山体受机器负载影响最大）。⚠ v2.8 起「河流 / 泉湖」两行合成「水面」一行 —— 合并后是**一次**拼几何 + 一次建材质，79ms 里含海面加密（每格 19 顶点） |
| 河流 | 7 条（**4 主河 + 3 支流**；汇流 0 处）；**沿程阶梯下降**（v2.8 阶段二）：源 1.406 → 海口 0.000、**落差 1.406 / 7 级台阶**、单调不升（0 处回升）、级间台阶摊在 `river.stepSlope = 1.0` 格的短斜坡内（不出现垂直台阶）、入海河河口钉在海面；槽深 2.20 |
| 河宽 | 全线等宽（1.000×），首尾差 0.000000；岸色带 湿岸 / 河床 / 漫滩 三档 |
| 水下地表（v2.6） | 深度 = **到最近陆地格六边形的连续距离**的函数（暴力对拍 1292 点误差 2.2e-16）；同一离岸档位内水深极差 0.297（旧版 0）；水/水共享边最大跨格差 0.0010；平台段 1642 **全部**贴岸（离岸 ≤ 内切圆半径）；最深 1.482 / 最浅 0.352；**地面网格 2054 个顶点最高 y = 0、高于水面 0 个**；外缘 182 个格边采样点全部 < 0（旧版全为 0） |
| 浅切槽 + 河岸走廊（v2.8 阶段二） | **154 个临河角点** + **98 条临河边中段**被切到水面之下（判据 `riverNear`）；横剖面 河线低于水面 79/79、半宽处仍在水下 75/75、**河岸台地** 28/28 不高于该处水面；临河平原格心最大高度 0.000000；水下地表在岸线处为 0（例外只有河口切槽与河岸台地各 3 处、最高 0.020，高于水面的 0 处） |
| 画面深度过渡 | 深度预通道半分辨率，74 个对象进通道 / **1 份水面材质**读同一份深度 uniform（v2.8 前是 3 份，现在海 / 河 / 泉已在同一份几何里）；水深由水面与床面重建后的世界 Y 差驱动，颜色为 `palette.water.shallow → palette.water.deep`（三类水各一档），alpha 为 `depthAlphaMin → 1`；**深度图采样 = 手动双线性**（`config.water.depthFilter`；相邻 4 texel 各自重建 `worldY` 后加权、背景权重置零，`'nearest'` 为对照档）、采样步长 = 1/**深度图**尺寸（1/640 × 1/400）；**海面**顶点色为常数（12934 个顶点中排除河 / 泉与河口湿岸 10034 个后，不一致 **0** 个）；**俯视对拍**：真实水深 1.482 → 重建 1.456、浅滩 0.352 → 0.357；**跨位姿对拍**：低俯角旋转与缩放、关雾同一水面像素极差 **Δrgb(2.6, 2.3, 2.1)**（开雾对照 84 / 76 / 75，刻意的雾；探针必须同时冻结 water 与 ambience 这两类动画输入，漏冻会随墙钟漂到 13~17），无 `uInvViewCos` |
| 手绘卡通水体材质（v2.7 → v2.8 统一） | 海 / 河 / 泉共用**唯一一份**自写 `ShaderMaterial`（`render/water-material.js`，旧 `stylized-water-shader.js` 已删除）；波浪做在**片元**（世界 XZ sum-of-sines + 解析偏导），振幅 ×hexSize（1.540 = 0.07 × 22）、波长 57.2，**同一段 GLSL 也驱动顶点位移**（只给开阔海面）；cel 色带 3 档 + 阶梯高光；泡沫按**真实水深**（带宽 海 0.660 / 泉 0.88 / 河 1.210，河最宽）；水纹贴图与顶点色**真正参与**颜色；三类水**逐顶点档案**各不同（深度淡出 海 1.980 / 河 1.210、水纹强度 河 0.55 …）；`aMouth` 在 **0 → 1** 之间（4 条主河有河口段、12934 个顶点里 min 0 / max 1） |
| 统一水面（v2.8：第一阶段合并几何，第二阶段逐地块高度） | 全场景 **1 个水面网格 + 1 份材质**（"不再有三张水面网格"）：**12934 顶点 = 海 3002 / 河 9288 / 泉湖 644**，18168 三角；海面按 `water.wave.subdiv = 2` 加密（每格 19 顶点）；`renderBias` 已彻底删除（`undefined`）；**海面几何严格落在海面水位**（3002 个海面顶点全部 y = 0，波动只在 GPU 上做）；**河面沿程阶梯下降**（v2.8 阶段二）：全图水面 y ∈ [0, 1.4064]、河面 y ∈ [0, 1.4064]、泉湖各用水位（0.00 / 1.32 / 1.35 / 1.41）、落差 1.406；顶点位移幅度 **1.540 单位 = 0.070 格**（上限 0.08）；河带末端**收尖** 57 个采样点（minScale 0.35）；河口延伸段 37 个采样点、末端离最近陆地 0.87~1.04 格；被河面压住的格边**不描边** 37 条 |
| 河口 / 分流 / 冲积（v2.7 → v2.8） | 4 条主河各生成 3 条分流带（共 **12 条 / 312 个顶点**：起点落在干流中心线上不露缝、半宽 0.55× 沿程递减、**逐点裁剪到水格内**、分流首点水位 = 干流分叉点水位（分段沿程单调不升）、起点与干流取自同一点）；冲积带只在 4 处河口附近为正（半径 4.8）、远处归零，**不改地形 / 高度 / 寻路**；⚠ 旧版那条 `renderBias 0.012`（0.264 单位）**已删除** —— 它就是河口台阶与「方头」的直接来源 |
| 河流规范（v2.7） | 水域连通域 **10 个**（最大 140 格，`seaMinBodyTiles = 12`）；4 条主河**全部** `reachesSea`（入海口所在水域 140 格，`majorSeaOnly = true` 未退化）；支流 3 条报 `confluence`、不给 `mouth`；1 条走不到水的轨迹按 `onDeadEnd = 'drop'` 丢弃；河口延伸段 37 个采样点**全部落在水格上** |
| 河源水体 | 4 处（3 湖 / 1 泉）+ 3 处跳过 = 7 条河；水面 644 顶点、**各水体自己的水位**（0.00 / 1.32 / 1.35 / 1.41，= min(该格档位, 碗沿自然地面最低值)）；半径 = 碗 0.72 格 × water（湖 0.62 / 泉 0.46）× 岸线扰动 ±10%；竖直射线 36 条被挡 0 条、落在水线上的坡脚 0 条；跨水面桥采样 2 个 |
| 水陆基色 | 水陆交界角 804 个，陆地顶点色与「纯陆地三格平均」最大偏差 **0**（对照组颜色差最大 0.354） |
| 统一地表（v2.9） | **1 个网格 + 1 份材质**（旧的五张分组网格 `landMesh / forestMesh / fieldMesh / flowerMesh / rockMesh` 已合并，别名删除）；逐顶点 splat 属性 2 条 vec4（`aSplatA` 4 槽 + `aSplatB` = 第 5 槽 / 岸线湿沙量）；逐槽位底色与**配置调色板逐分量偏差 0**（线性化后：`grass#96b35a = 0.305/0.451/0.102`、`forest#617f43 = 0.120/0.212/0.056`、`field#d5b36a = 0.665/0.451/0.144`、`flower#b47aa7 = 0.456/0.195/0.386`、`ridge#8f806d = 0.275/0.216/0.153`）；**陆地格心像素**代表格 `rgb(199,217,118)`、饱和度 **99**（修前「整图变白」时 `rgb(255,255,245)`、饱和度 10）；岸线湿沙混合带 0.9 格（`world.waterDistance` 连续距离场驱动）；渲染调用数 212 → **208**（三角面数不变） |
| 曲面无缝性 | 同 XZ 的重复顶点 y 逐位相等（**751 组**，最大差 0.00e+0）；共享顶点颜色 / 法线最大差均为 0.0e+0（**768 组**）。⚠ 组数比 v2.8 的 229 组大，是因为统一地表把原来只算「陆地网格」的口径扩到了整张地表（陆 / 林 / 田 / 花 / 岩同处一份几何）—— 判据没变，分母变了 |
| 城市 | 13 座全部落格，地形为 city |
| 道路 | 21 条（五档）；路面 / 桥 / 栈桥 / 隧道 分档统计 |
| 道路构件 | 桥墩 13 根、隧道洞口 14 个、洞内暗色路面 7 段；蜡笔描边 877 条边 × 3 段 = 2631 段笔触 / 0 处断笔；笔触宽度 2.21~5.88 单位；同一物理顶点的重合数 3508 ＝ 期望值（877 条边 × 每边 2 个内部站点 × 2 个顶点） |
| 植被道具 | 715 个（含边界过渡 208 / 格内次生特征 172（其中水洼 40）/ **泉湖沿岸 6**）；丘陵格内最高 3.69 单位（丘陵波峰） |
| 避让 | 落在山体格内的道具 0 个；落在河里的道具 0 个；压河 / 进山的房屋 0 栋 |
| 村落 | 13 城共 71 栋房屋 |
| 环境系统 | HUD 时段 4 档 / 季节 4 档 / 天气 4 档 + 自动轮播；切换会同时改地表与场景主色 |
| 环境图层 | 贴地云影 1 层（**贴合地形**，抽 253 个顶点相对地形偏移 min = max = 1.100 单位；默认关）、高空云雾 9 团（默认关）、飞鸟 14 只 |
| 确定性 | 同种子两次构建的地块签名、河道、道路采样数与分级统计完全一致 |
| 应用级重建 | 重建后场景根节点恒 11（无旧图层堆积）、旧图层组 `__hexLabDisposed`、Picker 与旅行模拟按新世界重建、仅改山体风格时河流 revision 不变、`mountainPass` 覆写后河段模式与河心侵蚀 1.000 生效、`dispose()` 后图层组残留 0 |
| 渲染 | WebGL 2.0，76240 三角面，**208** 次绘制调用（v2.9 统一地表后减 4） |

**渲染测试不产出任何文件**（不写图片、不建目录）。要看画面直接打开 `index.html`；`render-test.js` 只打印上面这些断言结果。

## 数据快照的更新

`src/data/world-snapshot.js` 内联了城市、道路、区域、viewBox、worldSchema 与地形种子。若原项目地图发生变化，把 `map/world-map.json` 里对应的几组数据重新拷贝覆盖该文件的常量即可，逻辑层（六边形世界、道路、寻路、渲染）无需改动。

## 已知边界

未实现的部分：网络层（无 WebSocket、无快照与增量同步）、格子事件与资源再生、交易与经济逻辑在 3D 地图上的接入、视锥剔除、真实美术资产、触屏与移动端适配。环境系统（日夜 / 季节 / 天气 + 自动轮播）与山体 LOD 已实现。磨损与生长有完整实现但按需求未启用演示。这些属于方案的阶段 2 及以后，详见方案文档 §11 与 §14。

热重建相关仍留在后续范围的有：旅行中玩家的位置 / 目的地 / 动画时间尚未迁移（当前重建会让玩家回到模拟初始状态）；`waterfall` 的垂直落差视觉与 `dryValley` 的干涸河床材质、生态规则尚未实现，两者目前只参与侵蚀与路径偏好；`blocked` 缺少专项回归断言；山体风格 `twinPeak` / `massif` / `ridge` / `valleyPeak` / `landmark` 已有计划层，但彼此的峰群几何差异仍可继续拉开。

地形侧另有两处是**明确留出、不是 bug**：`gorge` 现在只能把山壳切到「地表」（`depth` 是比例），做不出**河床低于平原的真正地下峡谷** —— 要做需要同时改 `heightAt` 的河道剖面与山壳切口，会动到「地表高度只有一个来源」这条红线，单独一轮再议；最粗一级 LOD（步长 7.33 单位 > 水带宽度 7.04）在俯视缩小时会把豁口采样成一两个顶点、山口变窄成一条缝，默认视角用的是 5 级（步长 4.40）不受影响。

**上一版挂着的几项，这一轮已经修掉（保留记录，避免重犯）：**

- **穿山兼容做好了却看不见（v2.4）**：数据层（`river.mountainErosion` / `MountainField.surfaceAt`）从 v2.2 起就能把山壳沿权威中心线切回地表，但**渲染网格**仍写着自己的老公式 `max(裸场, 地表)`，把切口重新盖了回去 —— 171 个河道横断面里 169 个被岩石压住，页面上就是「河钻进山就没了」。修法：网格改走共享的 `surfaceFrom`、存活单元判据改成「相对地表的厚度」（厚度≈0 处不发三角形，避免与地表共面闪面）、侵蚀剖面改成「水带内恒为核心开通度」（`pass.depth` 0.72 → 1.00）。**它能藏住的真正原因是两条断言盲区**：几何红线拿网格自己的公式验网格（自证），而射线可见性断言又把唯一可能盖住水的山体排除了。教训：查「特性有没有生效」要把链路走到产物为止，判据必须来自与实现不同的来源（详见方案 §15.21）。

- **雪线没跟上平面化**：旧写法是绝对阈值「高程 ≥ `maxRise` × 0.62」，而 `maxRise = 41.8` 已不代表任何真实地表，实测 53 座峰一个雪顶都没有。现在雪线按**该片自身峰高**取（`snowY = baseY + apexH × snowRatio`），再叠方向性噪声做成顺坡条带，实测 121 片全部有雪顶、`snowY / apexY = 0.80`。
- **山脉占比的分母不一致**：目标按「全部非边界格 447」算，实际挑选范围却是「非水 **且** 离城 ≥ 2 格」的可用集合，于是目标 34.4% 与实测 26.4% 天然对不上。现在 `targetByTerrain.ridge` 与挑选范围**同分母**（`ridgeShare × 可用格 / 非边界格`），实测 27.2% → 27.1%，断言通过。教训：份额目标的分母必须与实际挑选集合一致，否则调 `ridgeShare` 永远调不齐。

另外几处是刻意的取舍，不是遗漏：

- **顶点色的交融权重只能调格心**：角点顶点被 3 格共享，同一物理点在相邻地块上必须算出完全相同的颜色，任何「自身权重更大」或「把外圈并进来」的写法都会让三格各算一份（历史实测色差 0.07~0.12），六边形色阶重现。所以角点固定等权，可调项只有 `blendWeights.center`。
- **河道的「岸」只能用颜色表达，不能靠几何**：地表每格只有「格心 + 中环 + 6 个共享角点」13 个顶点，格边附近只有角点顶点，抬到水面之上的「岸唇」没有顶点承载（实测只有约 27% 的水带边缘能达到水面之上）。所以几何只负责把水下陷，两岸交给河床混色与湿岸带。要真正做出几何意义上的岸，必须提高地表网格在格边附近的细分。
- **岩石贴图用平面 UV**：山体与岩壁地表按世界 XZ 贴图，陡壁上的层理会顺着坡面拉伸。要做到严格的等比例需要三平面映射（triplanar），本期先用「少量层理 + 稀疏裂纹」把它压到可接受的程度。
- **洞口岩体是「加法」而非布尔切除**：低多边形风格下不做布尔运算，洞口用「卡在路上的六棱柱岩体 + 门框 + 两层暗面」拼出来。它读起来是「路从岩体里穿过去」，但岩体本身不会真的切削地表网格。
- **御道的细构件不做远景简化**：v1.6 已让扁带横向贴地（`ground + maxDrop`），但御道自身仍是 道砟面 + 双钢轨 + 枕木 + 两条边线 共 4~6 条带；掠射角下它们仍会读成一组平行的细线，只是不再悬空。若还需要更「干净」，下一步是按距离把细构件合并（远景只留路面 + 两条边线），本轮未做。
- **山体不加墨线描边（按需求决定）**：山体是独立网格、不与地表共享顶点，给它描边一定会在山脚的地表上多出**一圈闭合线**（旧版用「全部描边」的写法试过，山脚像被框住）。所以剪影只靠 `flatShading` 的块面感 + 分带顶点色，不加轮廓线。
- **山体格内只长碎石坡，不长植被**：山体的格内被几何占住，树从里面穿出来会很难看；山格因此只长岩石，并强制落在 `ecology.ridgeScree` 区间。⚠ 判据必须跟着山体的形状走：`MountainLayer.occupancy` 给出的 `thickness(x, z)` 就是「这一点上山的厚度」，用 `thickness < ridgeScreeClearance` 判，山脚坡面上就不会再冒出埋进坡里的岩石。<br>历史：旧版按「离格心距离」布置、并把内径与山体的中环半径对齐；改成「簇边界收脚」之后两者不再同源，碎石会整块埋进坡面 —— 这正是「判据的坐标系必须与被判对象的坐标系一致」的又一处。
